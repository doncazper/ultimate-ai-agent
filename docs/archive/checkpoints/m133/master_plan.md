# M133 Master Plan

1. Add contract-only, review-only Long-Running Task Supervisor records.
2. Bind M133 to Mode 5, M132 trusted workflow decisions, M131 scoped
   work-session decisions, supervisor plan refs, task refs, run-state refs,
   heartbeat plan refs, checkpoint plan refs, context budget refs,
   pause/resume/stop refs, audit/replay refs, revocation refs, kill-switch
   refs, and no-effect receipt refs.
3. Deny supervisor start, supervisor runtime, task supervision, heartbeat
   monitor, checkpoint scheduler, resume execution, recovery execution, human
   checkpoint scheduling, schedulers, background workers, execution, routes,
   controls, dependencies, beta release, and production authority.
4. Add tests, documentation-integrity checks, static safety checks, route
   boundary checks, and Foundation Gate criteria.
5. Keep M134 planned/provisional.

# v0.78.0 README Import

v0.78.0 implements M74 Browser Observe-Only Adapter.

Import into active README/status docs:

v0.78.0 adds a local browser observe-only adapter that accepts explicit
injected observation records and returns redacted visible text preview outputs
with safe refs only. It adds policy validation, request validation, result
contracts, redaction summaries, stable reason codes, tests,
documentation-integrity checks, static verification, and Foundation Gate
coverage.

It adds no browser automation, browser navigation, browser click, form fill,
screenshot, raw DOM, authenticated browser profile, cookies or credentials,
download or upload, remote browser, network interception, network call, model
call, tool execution, memory write, context injection, backend route, Control
Center control, dependency, M75 work, or production authority.

M75 remains future.

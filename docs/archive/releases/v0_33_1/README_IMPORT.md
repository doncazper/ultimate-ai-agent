# v0.33.1 README Import

Status: historical release artifact.

v0.33.1 hardens M29 — Agent Task Planning Engine.

It strengthens dependency graph validation, duplicate/missing step denial,
self/direct/indirect cycle detection, derived risk checks, hidden side-effect
denial, authority-boundary checks, evaluator revalidation, no-execution
invariants, static verification, documentation, and Foundation Gate coverage.

It adds no task execution, scheduler runtime, background worker, tool
execution, action execution, shell/subprocess execution, file mutation, memory
write, network call, model/provider call, browser/mobile/remote/plugin
execution, backend task/plan execution routes, Control Center execute controls,
dependencies, context injection, production authority, or M30 implementation.

M30-M40 remain planned/provisional.

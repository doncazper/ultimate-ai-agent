# v0.80.0 README Import

v0.80.0 implements M76 OpenWebUI Runtime Bridge v1.

Import into active README/status docs:

v0.80.0 adds deterministic review-only OpenWebUI runtime bridge envelopes over
safe refs only. It adds policy validation, request validation, receipt plans,
stable reason codes, tests, documentation-integrity checks, static
verification, and Foundation Gate coverage.

It adds no live OpenWebUI connection, OpenWebUI runtime call, OpenWebUI handoff
execution, provider call, model call, model authority, tool execution, memory
write, context injection, network call, credentials or cookies, raw prompt, raw
provider payload, raw content, backend route, Control Center control,
dependency, M77 work, or production authority.

M77 remains future.

# v0.96.0 Master Plan

v0.96.0 implements M92 Low-Risk Tool Autonomy, Single Session.

The milestone remains contract-first and review-only:

- define low-risk tool autonomy single-session policy, request, decision, and
  receipt plan contracts.
- bind exactly to M91 Autonomous Tool Execution Contract decisions.
- bind exactly to M69 low-risk autonomous dry run records.
- require safe refs only.
- require safe summary only receipt plans.
- revalidate safety-critical fields at evaluator boundaries.
- add tests, documentation-integrity checks, static verification, and
  Foundation Gate coverage.

The milestone adds no real tool execution, autonomous execution, session start,
additional session, multi-tool run, command execution, shell execution,
subprocess execution, filesystem mutation, network access, browser automation,
plugin execution, remote execution, model call, memory write, context
injection, background worker, backend route, Control Center control, dependency,
M93 work, or production authority.

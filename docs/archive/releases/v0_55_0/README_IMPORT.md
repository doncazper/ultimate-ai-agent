# v0.55.0 README Import

v0.55.0 implements M51 OpenWebUI Bridge Adapter Pilot.

The release adds a deterministic local safe-summary adapter boundary for future
OpenWebUI shell integration. Agent Core remains authority, OpenWebUI is not the
agent brain, and the adapter returns no raw prompt, raw provider payload, raw
content, or authority grant.

M52 remains future.


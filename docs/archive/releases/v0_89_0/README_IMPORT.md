# v0.89.0 README Import

Current active baseline: v0.89.0.

v0.89.0 implements M85 Read-Only Command Allowlist as contract-only,
review-only, deterministic, local-only safe metadata over exact M84 sandboxed
echo/no-op command decisions.

It adds no command execution, subprocess execution, shell execution, process
spawn, filesystem mutation, network access, tool execution, browser automation,
plugin execution, remote execution, model call, memory write, context injection,
background worker, backend route, Control Center control, dependency, M86 work,
or production authority.

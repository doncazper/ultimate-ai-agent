# README Import v0.37.3

Status: active release packet.
Current active baseline: **v0.37.3**

v0.37.3 repairs active roadmap label alignment after v0.37.2. It aligns the
planned `v0.38.0 / M34` label across active roadmap sources as `Broader File
Capability Review`.

The patch strengthens documentation-integrity checks so active
roadmap/currentness docs cannot disagree on the next planned milestone label.
Archived roadmap snapshots remain historical and are not treated as active
currentness sources.

The patch adds no M34 implementation, Broader File Capability Review
implementation, Safe File Review Workflow, macOS Local Companion, M35 work,
backend route, frontend feature, raw file read, context injection, memory
write, execution, dependency, or production authority.

OpenAPI path count remains `74`. M34-M40 remain planned/provisional.

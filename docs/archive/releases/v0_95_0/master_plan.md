# v0.95.0 Master Plan

M91 Autonomous Tool Execution Contract is a contract-only, review-only
milestone.

The release defines autonomous tool execution contract metadata over exact M90
Shell/Subprocess Hardening Freeze decisions. It records safe contract refs,
safe tool refs, safe execution scope refs, tool intent refs, tool runtime refs,
dry-run plan only semantics, and safe refs only receipt plans. It does not
implement real tool execution, autonomous execution, autonomous session start,
command execution, shell execution, subprocess execution, filesystem mutation,
network access, browser automation, plugin execution, remote execution, model
call, memory write, context injection, background worker, backend route, Control
Center control, dependency, M92 work, or production authority.

Foundation Gate, static verification, documentation integrity, and tests must
all preserve exact M90 binding, safe contract refs, no raw tool payload, no raw
provider payload, no raw prompt, safe summary only, and evaluator boundaries
revalidate safety-critical fields.

# v0.79.0 README Import

v0.79.0 implements M75 Browser Action Dry-Run Planner.

Import into active README/status docs:

v0.79.0 adds deterministic reviewable browser action dry-run planning contracts
over safe refs only. It adds policy validation, request validation, step
validation, receipt plans, stable reason codes, tests, documentation-integrity
checks, static verification, and Foundation Gate coverage.

It adds no browser action execution, browser session start, browser navigation
execution, browser click execution, form fill execution, screenshot, raw DOM,
authenticated browser profile, cookies or credentials, download or upload,
remote browser, network interception, network call, model call, tool execution,
memory write, context injection, backend route, Control Center control,
dependency, M76 work, or production authority.

M76 remains future.

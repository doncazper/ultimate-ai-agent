# v0.77.0 README Import

v0.77.0 implements M73 Browser Automation Contract Review.

Import into active README/status docs:

v0.77.0 adds contract-only, review-only browser automation contract review. It
defines future browser capability categories, treats observe-only browser
adapter work as an M74 candidate only, keeps all browser runtime authority
disabled, revalidates safety-critical fields at evaluator boundaries, and adds
tests, documentation-integrity checks, static verification, and Foundation Gate
coverage.

It adds no browser automation, browser observe, browser navigation, browser
click, form fill, screenshot, raw DOM, authenticated browser profile, cookies or
credentials, download or upload, remote browser, network interception, network
call, model call, tool execution, memory write, context injection, backend
route, Control Center control, dependency, M74 work, or production authority.

M74 remains future.

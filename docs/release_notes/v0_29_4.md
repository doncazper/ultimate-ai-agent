# Release Notes v0.29.4

Status: Current release notes.

## Summary

v0.29.4 repairs documentation archive references and makes the documentation
organization policy self-maintaining after v0.29.3.

## Changed

- Archives active-looking historical version verifiers for v0.5.4, v0.5.5,
  v0.5.6, and v0.5.8 under their matching `docs/archive/releases/` packets.
- Removes stale Ruff excludes for retired historical verifier paths.
- Adds `docs/maintenance/DOCUMENTATION_ORGANIZATION_POLICY.md`.
- Updates active docs indexes, canonical map, roadmap, route inventory, test
  strategy, release packet, and Foundation Gate implementation plan for v0.29.4.
- Strengthens documentation-integrity checks so active validation fails if
  historical version verifiers reappear at root or under active `scripts/`.
- Keeps historical release artifacts preserved under `docs/archive/releases/`.

## Safety

v0.29.4 is documentation organization repair only. It adds no M26 Grounded
Recall Router, Context Pack Builder, backend routes, frontend features,
runtime/model/provider calls, retrieval/RAG/vector/embedding functionality,
memory writes, tool execution, dependencies, security architecture changes, or
production authority.

OpenAPI path count remains `74`. M26 remains planned/provisional.

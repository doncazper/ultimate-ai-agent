# v0.103.0 Release Notes

Status: current release notes.

v0.103.0 is the Founder Loop V1 currentness, branch-hygiene, and baseline
consolidation release. It accepts the current `main` branch as the active
product/package baseline after the completed FCC-V1 proof-lane conveyor and
the repo-currentness documentation pass.

## Scope

- Promote the active product/package baseline to `v0.103.0` / `0.103.0`.
- Record `checkpoint-m169` as the latest accepted repository checkpoint.
- Keep `checkpoint-m166` and `checkpoint-m167` as the latest accepted local
  model lane checkpoint tags.
- Consolidate README, VERSION, docs index, canonical map, roadmap docs, product
  truth, security posture, API boundary docs, Control Center route-status
  manifests, and verification milestone status under the new baseline.
- Preserve the completed bounded FCC-V1 proof-lane truth: FCC-V1-000 through
  FCC-V1-007 are complete for release-surface truth, API perimeter,
  backend-owned Action/Chat/Memory decisions, Evidence Timeline
  productization, and proofed `/actions`, `/chat`, `/memory`, and `/evidence`
  route surfaces only.
- Keep full UAA-P1-087.2 private UI functional tuning, UAA-P1-087.3 native
  SwiftUI boot cockpit planning, broader product-readiness claims, and
  provider/packaging/commercialization expansion deferred.
- Record branch hygiene: `main` is current, with PR #35 and PR #36 intentionally
  left as the only open non-merged branches.

## Non-Goals

v0.103.0 does not add production authority, public distribution, public beta,
signed installer readiness, hosted deployment, runtime model/provider calls,
unrestricted web fetching, unrestricted browser automation, shell/subprocess
authority, connector writes, account auth, email/calendar reads, plugin runtime
import, arbitrary plugin execution, mobile control, memory writes, context
injection, raw prompt export, raw provider payload export, credential handling,
or raw private-content persistence.

## Verification Notes

This baseline should be verified with the version truth checker, documentation
integrity verifier, OpenAPI verifier, product-truth verifier, Control Center
frontend safety verifier, pytest collection, and `verify_all.py --skip-pytest`.

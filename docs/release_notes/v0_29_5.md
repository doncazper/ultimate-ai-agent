# Release Notes v0.29.5

Status: Current release notes.

## Summary

v0.29.5 accepts a tiny documentation organization policy polish after v0.29.4.

## Changed

- Accepts pushed commit `374bb1e`, which removes duplicated wording from
  `docs/maintenance/DOCUMENTATION_ORGANIZATION_POLICY.md`.
- Adds v0.29.5 release notes, archive release packet, and Foundation Gate plan
  references so the cleanup can serve as an explicit pre-M26 baseline.
- Keeps v0.29.4 archive reference repair and documentation-integrity guardrails
  intact.

## Safety

v0.29.5 is documentation policy polish only. It adds no M26 Grounded
Recall Router, Context Pack Builder, backend routes, frontend features,
runtime/model/provider calls, retrieval/RAG/vector/embedding functionality,
memory writes, tool execution, dependencies, security architecture changes, or
production authority.

OpenAPI path count remains `74`. M26 remains planned/provisional.

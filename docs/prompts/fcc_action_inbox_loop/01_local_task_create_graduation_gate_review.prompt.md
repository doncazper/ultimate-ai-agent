# FCC-ACTION-INBOX-LOOP-001a Local Task Create Graduation-Gate Review

Role: You are a Principal Software Engineer performing an adversarial,
production-grade graduation-gate review.

Mode: review first. Implement only the smallest hardening fixes required for
the gate to be truthful. Do not broaden authority.

Task: Re-run the `local_task_create` Action Inbox graduation gate from a live
readability and receipt-visibility standpoint after the mock/degraded
truth-binding fix.

Gate question:
Can an operator safely understand, from live `/actions`, whether a
`local_task_create` item is eligible, approved, blocked, pending receipt,
committed, replayed, or conflicted from backend-owned data only?

Evaluate:
- `approval_envelope` and `receipt_visibility` are generated by Python
  core/API read models.
- React does not mint action kind, exact scope, risk class, side-effect class,
  approval requirement, eligibility, grants, receipt refs, replay posture, or
  conflict posture.
- Mock/degraded data is explicit and non-authoritative.
- Commit control appears only for backend-owned, approved, unexpired,
  exact-scope `local_task_create` items.
- Backend validates `approval_ref` through `LocalApprovalAuthority`.
- Missing approval, forged grants, wrong kind, wrong scope, stale state,
  expired state, idempotency replay, and idempotency conflict are tested.
- Route metadata, OpenAPI, API manifest, CLI parity, docs, and tests agree.
- Receipts/evidence use safe refs only.

Required checks:
- `.venv/bin/python scripts/verify_operational_maturity.py`
- `PYTHONPATH=src .venv/bin/python -m pytest tests/test_operational_maturity_manifest.py -q`
- `PYTHONPATH=src .venv/bin/python -m pytest tests/test_founder_loop_storage.py -q`
- `PYTHONPATH=src .venv/bin/python -m pytest tests/test_control_center_api_routes.py -q`
- `PYTHONPATH=src .venv/bin/python scripts/verify_openapi_contract.py`
- `.venv/bin/python scripts/verify_documentation_integrity.py`
- `make frontend-check`

Output:
- gate decision: PASS, PASS WITH CONDITIONS, or FAIL
- evidence supporting the decision
- any in-scope fixes made
- tests/verifiers run
- remaining risks before the next exact local task step


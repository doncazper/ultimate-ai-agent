# Control Center Operator Shell Gap Map

Status: active UAA-P0-007 operator-shell gap map
Baseline: v0.104.0 / 0.104.0
Source plan: `docs/roadmap/OPERATOR_RUNTIME_EXCELLENCE_ROADMAP.md` M172
API boundary: current FastAPI manifest has 348 OpenAPI paths
Lineage: carries forward the UAA-P1-065 `FCC-P0-002` Control Center
organization cleanup as historical product-surface context.

This map is operator-readiness and release-claim discipline scaffolding for the
Control Center operator shell. It does not add runtime authority, backend
routes, frontend controls, shell/subprocess execution, browser automation,
connector writes, plugin runtime import, mobile control, autonomous background
execution, or public distribution.
`docs/control_center/BACKGROUND_AUTONOMOUS_PROVIDER_CALLS_PROMOTION_PLAN.md`
is planning-only: Control Center has no background provider queue dispatch,
scheduler control, autonomous provider-call control, billing authority toggle,
broad provider router toggle, or new API runtime route for that capability.

Control Center and OpenWebUI remain shells. Python Agent Core, PolicyEngine,
LocalApprovalAuthority, route side-effect classification, OpenAPI checks, and
Foundation Gate checks remain the authority boundaries.

Authority posture is migrating from legacy one-off lane promotion language to
AuthorityLease V1: operator-selected trust modes, explicit domains,
session/mission scope, constraints, receipts, audit, redaction, rollback,
safe-disable, and kill-switch visibility. The current inspection surfaces are
`GET /api/runtime/authority-state`,
`GET /control-center/settings/status#authority_lease_state`, and
`scripts/dev/uaa_runtime.py inspect-authority-state --json`; the compact
operator parity view is
`scripts/dev/uaa_runtime.py inspect-authority-state --summary` plus
`authority_lease_state.mode_catalog` and `authority_lease_state.decision_summary`,
which expose mode readiness, default requested domains, capability totals,
outcome counts, blocked reason refs, and unsupported adapter refs without
execution or mutation. Operator-selected
session leases are recorded through `POST /api/runtime/authority-leases`,
`POST /api/runtime/authority-leases/approve-and-issue`,
`POST /api/runtime/authority-leases/revoke`,
`scripts/dev/uaa_runtime.py select-authority-mode --approve`, and
`scripts/dev/uaa_runtime.py revoke-authority-lease`. Control Center `/settings`
can request and revoke implemented local lease subsets with receipts. These
routes are the mapped `system_settings/write` authority control plane and do
not execute adapters, bypass unknown-authority denial, or grant unsupported
domains. Unsupported browser, app, payment, calendar, messages, email,
contacts, Home Assistant, and production/cloud adapters remain blocked or
draft-only until implemented and tested, but the operator-facing explanation
should name the required mode and domains instead of opaque blocked-language
without a lease/domain/capability reason. AuthorityLease capability mappings
now include at least one row for every target domain, with unsupported rows
marked planned or blocked instead of hidden.
Governed Runtime command policy uses the active lease store: read-only status
commands require `workspace/read`, and execution-capable command capabilities require
`workspace/execute` plus the exact approval envelope and receipts.
Work Board persisted reorder and local-card-create mutations now require
`workspace/write` plus exact approval, idempotency, receipts, redaction, and
rollback/safe-disable refs.
Action Inbox approve/edit/reject/defer decision receipts now require
`workspace/write`, idempotency, receipts, redaction, audit refs, and
rollback/safe-disable refs before local decision state is recorded; missing
authority is blocked as receipt state and does not mint backend approval.
Action Inbox local-task commit now requires `workspace/write` plus exact
approval, idempotency, receipts, redaction, and safe-disable refs before local
Founder Loop task state is written.
Memory Review accept/correct now require `memory/write` plus exact approval,
idempotency, receipts, redaction, and rollback/safe-disable refs before a
reviewed recall-only local memory record is written.
CRM local mutations now require `contacts/write` plus exact approval,
idempotency, receipts, redaction, and rollback/safe-disable refs before local
CRM state is changed.

The `context_injection` prerequisite contract is planned-only and
contract-ready for future gate review. It adds no runtime prompt/context
injection route, prompt context writing, OpenWebUI handoff execution, backend
mutation route, Control Center injection control, provider/model call,
connector write, action execution, public beta, public release, production
readiness, or production authority.

UI surface direction: Control Center / Founder Command Center is the
first-party product cockpit for Today, Inbox, Plans, Actions, Memory, Evidence,
Settings, Models, and future first-party Chat. OpenWebUI remains a supported
local/dev conversational shell and compatibility surface for governed `/v1`
smoke, llama.cpp shell testing, and developer chat. OpenWebUI must not become
the source of product state or the destination for wiring every UAA workflow.
The current visual north star for the Founder Command Center is recorded in
`docs/portfolio/PRODUCT_NORTH_STAR.md`. An isolated code-native review lane now
implements the current surface geometry under `/workspace/*`, including the
immersive Studio representation. Messenger navigation resolves to the existing
canonical fixture-only `/messenger` shell rather than a competing workspace
client. Its Intelligence inspector renders the backend-owned MSG-MX-010 partial
posture: exact local policy/context/proposal lanes and explicitly blocked
provider/attachment families. Room/message content remains synthetic, and the
shell mints no approval, lease, provider, attachment, send, action, context, or
Memory authority. The canonical routes
remain unchanged until compatible backend contracts and route truth are ready
for an explicit cutover. These representations do not promote any route,
workflow, connector, model/provider call, messaging send, skill activation, or
authority class beyond the current route/API/test evidence in this map.
The accepted legacy `01`–`19` PNG pack is also built as an isolated,
fixture-backed `/workspace/reference/*` review lane. Its content controls are
disabled and visibly marked `Not backend-wired`; only reference navigation is
active. The pack is a visual implementation target, not a second state owner,
route cutover, capability promotion, or claim that its workflows are UI
implemented.
`/private-trial` is the UAA-P1-087.2a/087.2b/087.2c read-only packet,
acceptance ledger, and unanswered manual-review scaffold surface only; it
records safe manual smoke checklist refs, manual smoke step refs, pending
surface review refs, acceptance question refs, tuning decision refs, unanswered
pending answer refs, missing implementation refs, friction refs, UI/copy task
refs, core-loop gap refs, and blocked authority refs without backend routes or
runtime authority. Full UAA-P1-087.2 private UI tuning is deferred until more
Founder Loop implementation exists and accepted or revised local/private
findings are recorded later.

Daily-loop product behavior now uses the existing Founder Loop read routes to
make Morning Briefing the daily home and Today the decision view. The Python
Agent Core summaries expose source-readiness states, CRM-lite follow-up refs,
memory "why shown" rows, Action Inbox review groups, Weekly Review narrative
refs, and private dogfood capture refs for local display. This is safe-ref,
review-only, draft-only, and local-only product behavior; it does not add
email/calendar access, account auth, background polling, connector runtime,
external writes, action execution, automatic memory truth, hidden context
injection, model/provider calls, public beta, distribution, or production
authority.

Parity gap-closure Phase 02 adds a short-lived, revision-bound Python Core
truth envelope at `GET /control-center/backend-truth` with CLI parity through
`scripts/dev/uaa_founder_loop.py inspect-backend-truth`. Start Here, Today,
Plans, Action Inbox, Approvals, Work Board, Morning Briefing, Memory,
Evidence/Proof, Setup, Chat handoff, active-run status, and Settings wait for that
envelope and their exact backend-owned route read models before rendering.
The standard local launcher injects the exact clean source revision. The
documented Compose stack requires that same verified clean revision at build
and runtime, while direct CLI truth inspection derives it only from an exact
clean repository root. During first run, `/actions` is the sole admitted
mutation bootstrap surface; every critical browser mutation is bound to the
admitted backend revision and process before route execution. Durable
completion proof is derived from coherent local-task receipts and proof
records rather than a seeded fixture identity, and any corrupt claimed receipt
invalidates the complete evidence set.
Missing, malformed, stale, out-of-order, mock-filled, or corrupt durable
evidence fails closed with bounded provenance and retry guidance. A real local
Python/backend browser walkthrough proves backend stop, durable restart, CLI/API
parity, and receipt-corruption handling. This read boundary grants no runtime,
model/provider, browser/web, connector, shell, background, or production
authority.

Beta 07 Trust authority map makes `/trust` a backend-owned authority map over
enabled, review-only, approval-required, planned, and blocked lanes from
`GET /control-center/trust-authority/matrix` and
`scripts/dev/uaa_founder_loop.py inspect-trust-authority`. It exposes CLI
inspection refs, safe-disable refs, rollback refs, promotion-path refs, proof
refs, verifier refs, and fail-closed frontend validation through
`scripts/verify_beta_07_trust_authority_map.py`. This is repo-safe read-model
hardening only. No broad runtime authority is added, and connector writes/sends,
provider/model calls, shell/subprocess execution, browser automation, runtime
context injection, standing authority, background autonomy, public release, and
production authority remain blocked until a later accepted AuthorityLease-gated
capability proves the matching mode, domain, receipts, rollback/safe-disable
posture, and tests.

Beta 11 Operator Workspace Spine adds one composed, backend-owned read-model
posture across Today, Proof, Trust, and CLI inspection for workspace status,
Git posture, preview status, run-log posture, and coworker handoff metadata.
It is not a file editor, terminal, Git client, browser controller, dev-server
manager, or coworker dispatcher. The current surface stores safe refs only and
keeps file writes, patch apply, Git mutation, shell/subprocess execution,
browser automation, provider/model calls, connector writes, raw path/log
persistence, background autonomy, public release, and production authority
blocked.

API boundary hardening gap: because Control Center is browser-facing,
UAA-P1-080 now makes public/protected route posture explicit before
authority-heavy product claims. Existing partial coverage includes OpenAPI/API
manifest metadata, route side-effect classes, route-status auth posture,
P1-080 route classification as `public_metadata`, `local_readonly`,
`local_sensitive`, or `mutating_requires_authority`, UAA-P1-081 centralized
response security headers, disabled-by-default bearer-gated local `/v1`
behavior, UAA-P1-082 explicit loopback CORS allowlist, UAA-P1-083 fail-closed
local protected-route bearer gate with an explicit local-dev bypass, and idempotency concepts in durable
run/action planning. UAA-P1-084 now adds a mutating-route idempotency header
gate without durable dedupe, exactly-once execution, replay execution, rate
limits, mutation authority, or production authority. UAA-P1-085 now adds
targeted local fixed-window rate limits without auth, distributed quota,
dependencies, billing, or production authority. UAA-P1-086 now adds OpenAPI/
API manifest/route inventory enforcement tests without route, middleware, or
runtime authority changes. CORS is browser hardening, not auth.

CLI is a first-class operator surface. The Control Center may expose a workflow
only as a shell over the same Python core/API contract that can be inspected or
operated from a command-line or repo-local script path. Product behavior must not
live only in React state; UI-only state is limited to presentation concerns such
as filters, expanded panels, selected tabs, and layout preferences.

Operational maturity is now an AuthorityLease capability maturity gate enforced by
`docs/control_center/operational_maturity_manifest.json`,
`docs/control_center/OPERATIONALIZATION_LADDER.md`, and
`scripts/verify_operational_maturity.py`. No module or AuthorityLease
capability may move from planned, proposal, partial, or proofed-review posture
to operational behavior without a manifest rank change, verifier pass,
backend-owned receipts, active lease requirements, exact mode/domain/capability
scope, and the required CLI/core/API parity evidence. Rank 2+ backend-owned
status routes now also require a manifest `ui_status_binding` so Control Center
UI/API copy cannot say Settings, Local Models, or future status modules are
missing, mock-only, or not wired while backend status exists; explicitly
backend-only status must carry a reason, doc ref, and blocker ref.
Future authority classes are governed by the AuthorityLease foundation in
`docs/strategy/UAA_AUTHORITY_MODES_AND_MISSION_LEASES.md`, ranked by
`docs/control_center/authority_candidate_scorecard.json`, and explained in the
legacy-stable conveyor doc `docs/control_center/AUTHORITY_RAMP_CONVEYOR.md`.
That scorecard is also a capability gate: no connector write, memory write,
shell/subprocess, browser automation, provider/model authority, or context
injection capability is selected until the candidate has exact mode, domain,
capability, active lease requirement, approval, idempotency, receipt/evidence,
rollback or safe-disable, redaction, CLI/API/core parity, and focused test
refs. The current scorecard decision is no-go for new authority; the existing
Action Inbox `local_task_create` remains the only rank 5 local write authority
capability.

Evidence sources for this map:

- `apps/control-center/src/routes.tsx`
- `apps/control-center/src/api/endpoints.ts`
- `src/ultimate_ai_agent/api/app.py`
- `src/ultimate_ai_agent/api/manifest.py`
- `tests/test_control_center_api_routes.py`
- `docs/control_center/CONTROL_CENTER_FRONTEND_ROUTES.md`
- `docs/control_center/ROUTE_STATUS_MANIFEST.md`
- `docs/control_center/route_status_manifest.json`
- `docs/control_center/OPERATIONALIZATION_LADDER.md`
- `docs/control_center/operational_maturity_manifest.json`
- `docs/control_center/AUTHORITY_RAMP_CONVEYOR.md`
- `docs/control_center/authority_candidate_scorecard.json`
- `docs/control_center/PRODUCT_LANGUAGE_RULES.md`
- `docs/portfolio/PRODUCT_NORTH_STAR.md`
- `docs/roadmap/PRODUCT_RELEASE_TRUTH_PACKET.md`
- `docs/production/M167_LOCAL_MODEL_E2E_SMOKE_HARNESS.md`
- `docs/production/LLAMA_SERVER_PACKAGING_PROVENANCE_CHECKLIST.md`
- `docs/production/LOCAL_MODEL_OPERATIONAL_RUNBOOK.md`
- `docs/production/RELEASE_LATENCY_BASELINE_HARNESS.md`

## Route Classes

The current API manifest classifies route side effects as:

| Class | Meaning for operator shell mapping |
|---|---|
| `none` | System metadata such as `/health`, `/version`, `/api/manifest`, and governed capability status. |
| `validation_only` | Review, preview, validation, and summary routes that do not execute production work. |
| `local_dev_workspace_only` | Local-only workspace or local gateway routes. These are not broad production authority. |
| `governed_network_read_only` | UAA-P1-063 allowlisted HTTPS GET evidence route with bounded redacted preview and receipt refs only. |

## Surface Matrix

Product Loop 009 Chat to loop handoff note: Today, Action Inbox, Morning
Briefing, and Chat can expose the backend-owned
`chat_to_loop_handoff_read_model` over existing safe Chat turn and handoff
receipt refs. The read model is proposal-only and fail-closed; it adds no model
output authority, direct memory write, automatic memory write, context
injection, action execution, plan execution, connector write, provider/model
call, browser execution, shell execution, public beta, or production authority.

Turn Contract Router productization note: Chat now exposes Router Diagnostics
as a Control Center presentation surface over the backend-owned
`POST /control-center/turn-router/preview` no-effect read model. The panel
shows selected turn contract, policy posture, reason refs, blocked-authority
refs, and no-effect proof for protected samples and one-shot ephemeral preview
text. It labels fallback as non-authoritative, fails closed when free-form
preview is unavailable, and is covered by `make frontend-turn-router-smoke`.
Durable workflow truth remains in Python Agent Core contracts and receipts, not
React state. The route and UI add no provider/model call, memory body retrieval,
memory write, tool/action execution, shell/subprocess execution, browser
automation, connector write, public beta, public release, or production
authority.

Product Loop 010 Evidence Timeline narrative note: Evidence can expose the
backend-owned `narrative_read_model` over existing Evidence Timeline safe refs
so what happened, why it was recorded, approval posture, what changed, what
remains blocked, and inspection refs are readable. The narrative is safe-ref
only and fail-closed; it adds no approval authority, no rollback execution, no
action execution, no connector write, no provider SDK calls, no provider/model
call, no live web, no prompt/response/provider exchange storage, no memory
truth authority, no context injection, no browser or shell execution, no public
beta, and no production authority.

Product Loop 010 remains no public beta and no production authority; it is
read-only local evidence narrative posture.

Product Loop 011 Settings and kill-switch clarity note: Settings can expose
backend-owned blocked/degraded/partial posture labels for web, providers,
connectors, memory context use, model runtime, local model lifecycle, and
platform capabilities through `GET /control-center/settings/status` and
`scripts/inspect_settings_authority_posture.py`. This is metadata visibility
only: no toggles that grant authority, no provider configuration, no installer
behavior, no runtime activation, no feature-flag writes, no kill-switch
execution, no revocation execution, no connector runtime, no connector writes,
no model calls, no provider SDK calls, no live web, no shell/browser execution,
no public beta, no production readiness claims, and no production authority.

Skill Workbench discovery and adoption note: `/studio/skills` now implements a
partial read-only Discover surface over the existing Python Core
skill-marketplace posture plus a sanitized, source-derived metadata snapshot.
List/grid selection, search, filters, selected item, and 25-row pagination are
presentation state. ClawHub stars/downloads/rank remain discovery signals;
missing Hermes ratings and popularity aggregates render as unavailable. The
list omits repeated license and guessed-risk columns, while the inspector keeps
source license detail and reports risk as not assessed. Saved, adaptation,
local-skill, blocked/rejected, and live-source lanes remain unavailable.
External metadata is not trust, install authority, runtime import authority,
execution authority, or local enablement. Future live metadata fetch must route through
WebAccessGateway; external code remains untrusted reference material until
rewritten or adapted into reviewed UAA-owned material under later exact
authority.

Product Loop 012 Private product loop trial script note: the repo now has a
local/private, safe-ref-only manual operator review checklist and acceptance
ledger for Boot, Today, Morning Briefing, Follow-Ups, Memory, Actions, Plans,
Chat Handoff, Evidence, Weekly Review, and Settings through
`docs/control_center/private_product_loop_trial_script_v1.json` and
`scripts/inspect_product_loop_trial_script.py`. Findings remain pending manual
operator review and the Product Loop Completion Report remains evidence-backed
gap reporting only: no public beta, no public distribution, no telemetry
export, no connector runtime, no connector reads/writes, no provider/model calls, no
provider SDK calls, no live web, no shell/browser execution, no action
execution, no memory writes, no backend route, no runtime authority, and no
production readiness claims, and no production authority.

UAA-P2 Agent Runtime Compatibility note: the repo now has a contract-only
compatibility layer for future agent runtimes in
`docs/architecture/UAA_P2_AGENT_RUNTIME_COMPATIBILITY.md` and
`src/ultimate_ai_agent/core/agent_runtime/`. This is Python Agent Core
infrastructure only: no Control Center surface, backend route, OpenAI Agents
SDK adoption, provider SDK import, runtime model/provider call, live web fetch,
browser automation, shell/subprocess execution, connector write, memory write,
context injection, plugin runtime import, action execution, public beta, public
release, production authority, or broad autonomy is added.

| Surface | Current frontend component/page | Current backend route(s) | Missing backend route(s) | Authority boundary | Side-effect class | Approval requirement | Evidence/audit output | Readiness status | Production-readiness blocker |
|---|---|---|---|---|---|---|---|---|---|
| Today | `/today` renders the storage-backed first-loop summary plus UAA-P1-068 Today Product Spine Contract fields, UAA-P1-069 Evidence History Grammar fields, UAA-P1-073 Plans Action envelope posture, UAA-P1-074 Chat local operator truth, UAA-P1-075 Governed Code Workbench metadata, UAA-P1-076 Cross-Surface Memory Intake proposal refs, UAA-P1-077 Memory-To-Loop Binding refs, UAA-P1-078 Private Beta-Readiness Gate refs, UAA-P1-079 User Intent Understanding refs, FCC-V1-003 workspace/draft AuthorityLease-gated Today-to-Action envelope creation, FCC-V1-004 Chat receipt/handoff refs, FCC-V1-005 Memory Review decision receipt refs, FCC-V1-006 productized Evidence Timeline event groups, and FCC-V1-007 proofed route-surface refs for Actions, Chat, Memory, and Evidence. | `GET /control-center/today/summary`, `POST /control-center/today/action-envelope`, `GET /control-center/memory/review`, `GET /control-center/evidence/timeline`, `POST /control-center/memory/review/{candidate_ref}/accept`, `POST /control-center/memory/review/{candidate_ref}/correct`, `POST /control-center/memory/review/{candidate_ref}/reject`, `GET /control-center/storage/status`. | Email/calendar read-only contracts, notification contract, private beta rehearsal receipts, and post-P1-086 exact workflow evidence. | Founder Loop storage summaries and review-only Action envelope creation; Today-to-Action promotion requires active `workspace/draft` AuthorityLease scope and records authority decision refs before local state is written; loop visibility is necessary but not sufficient for completion; evidence history, Action envelopes, Chat local operator truth, durable Chat receipt/handoff refs, governed Code workbench metadata, cross-surface memory intake proposal refs, memory-to-loop refs, private beta-readiness refs, user intent refs, and backend-owned Memory Review decision receipt refs and productized Evidence Timeline events are safe-ref posture; no action execution, apply execution, handoff execution, connector writes, CRM/account sync, provider calls, email/calendar reads, notification delivery, rollback execution, reusable approval grant authority, memory write, context injection, public beta, distribution, or production authority. | `local_dev_workspace_only`. | None for read-only summary; Today-to-Action envelope creation requires active `workspace/draft` AuthorityLease scope, idempotency, rollback/audit posture, receipt refs, and authority decision refs. Chat receipt/handoff recording, Memory Review decision receipt recording, and any later mutation require exact LocalApprovalAuthority posture where applicable, idempotency, rollback/audit posture, and receipt refs. | SQLite state refs, JSONL log refs, Today-to-Action envelope authority decision refs, Today-to-Action envelope receipt refs, Chat turn receipt refs, Chat handoff receipt refs, Memory Review decision receipt refs, safe evidence refs, bounded summaries, blocked states, backup manifest refs, UAA-P1-068 through UAA-P1-086 and FCC-V1-003/FCC-V1-005/FCC-V1-006/FCC-V1-007 contract/schema/verifier/test refs, checked-in visual baselines, route classification refs, and local loopback packaging proof refs. | Partial: UAA-P1-068, UAA-P1-069, UAA-P1-073, UAA-P1-074, UAA-P1-075, UAA-P1-076, UAA-P1-077, UAA-P1-078, UAA-P1-079, UAA-P1-080 through UAA-P1-086 perimeter contracts, FCC-V1-003 first vertical slice, FCC-V1-004 Chat receipt/handoff, FCC-V1-005 Memory Review decisions, FCC-V1-006 Evidence Timeline productization, and FCC-V1-007 proofed route-surface promotion for Actions, Chat, Memory, and Evidence are implemented; product-readiness claims remain blocked. | Integration contracts, notification contracts, memory write policy binding, private beta rehearsal receipts, and full API/Foundation Gate extraction remain future work. |
| Inbox | `/inbox` renders a blocked/planned Founder Command Center communication triage posture plus the dedicated backend-owned Source Readiness read model for inbox, calendar, tasks, CRM-lite/manual notes, repo, and local-file lanes using explicit `ready`, `blocked`, `metadata_only`, and `not_configured` labels. The same backend read model now exposes draft-only proposal candidates for email metadata contract, calendar metadata contract, account-auth boundary work, and implemented connector draft proposal refs for email-response and calendar-hold review. The Inbox page still does not bind to live email, calendar, account, polling, connector runtime, send, or write behavior. | `GET /control-center/sources/readiness`, with the same readiness embedded in `GET /control-center/today/summary`, `GET /control-center/morning-briefing/summary`, proposal-only Action Inbox read-model items in `GET /control-center/actions/inbox`, and CLI inspection through `scripts/inspect_connector_draft_proposals.py`. | Receipt-backed source-readiness proposal handoff, connector account/read-metadata contracts, test-account source proof, and any live source ingestion contract. | Read-only source readiness posture and proposal-only connector draft refs; no connector runtime, connector reads/writes, account auth, auth-material handling, message send/archive/delete/label/move, raw message/calendar metadata display, memory writes, context injection, model/provider calls, background fetch, notification delivery, or production authority. FCC-P1-007, FCC-P1-008, and FCC-P1-009 are contract-only Python-core metadata/proposal envelopes; connector draft-only proposals are local safe-ref review artifacts. | Dedicated source readiness is `local_dev_workspace_only` read-model data; `/inbox` and `/actions` remain shells over backend-owned status/proposal projections. Future connector metadata routes must declare read-only/validation-only side-effect classes before UI controls appear. | No approval for read-only source-readiness inspection or connector draft proposal review. Future triage, draft proposal receipts, test sends/writes, or connector actions require exact scope, LocalApprovalAuthority boundary, idempotency, rollback/safe-disable posture, audit, and receipt refs. | Source-readiness safe refs, missing contract refs, blocked-state refs, blocked authority refs, source status labels, proposal refs, connector draft proposal refs, proposal-only Action Inbox item refs, route refs, product spec and gap-map refs, plus contract-only calendar/email/draft proposal test evidence; no connector receipts, account proofs, raw source evidence, sent draft, or completion evidence. | Operational maturity scorecard: Inbox/Sources is rank 2 proposal review for backend-owned source-readiness candidates and connector draft proposal refs with execution blocked. No connector runtime, send, write, or account authority is claimed. | Receipt-backed proposal handoff, connector/account metadata contracts, test-account source proof, and frontend tests proving no send/write authority remain future scoped. |
| Action Inbox | `/actions` renders backend-classified queue states from local Founder Loop storage: `ready_for_decision`, `approved_local_task_lane`, `blocked_by_authority`, `expired_stale`, `receipt_recorded`, and `proposal_only_no_execution_path`. FCC-INBOX-001 adds backend-owned `approval_envelope` and `receipt_visibility` read models/cards per item so action kind, exact scope, risk, side-effect class, approval requirement, expiry/staleness, idempotency, expected receipt refs, rollback/safe-disable posture, blocked authority refs, evidence refs, decision receipt refs, local task refs, local task commit receipt refs, AuthorityLease decision refs, Evidence Timeline event refs, replay posture, and conflict posture use one consistent grammar. FCC-ACTION-002 keeps filters/drilldowns presentation-only, refreshes the Action Inbox read model after a local task commit, and lets the card move to `receipt_recorded` only after backend-owned receipt visibility confirms the commit. Governed Runtime Phase 07 adds a backend-owned runtime Action Inbox bridge read model for focused pytest, repo-verifier, frontend-check, and repo-doctor command capabilities with envelopes, receipts, status/readiness, CLI refs, safe-disable posture, evidence timeline events, command root-pinning posture, configured endpoint matching, receipt-detail truth, and approval preflight only. Provider credential, CostGovernor, credential-validation, and provider invocation posture is shown as backend-owned blockers only: configured/not-configured/revoked state, unknown paid-cost approval, provider/model refs, cost estimate refs, budget decisions, max-approved USD refs, validation receipt refs, invocation receipt refs, authority-required, approved-no-execution, validation-blocked, live-adapter-blocked, and no-provider-authority labels do not authorize provider use. | `GET /control-center/actions/inbox`; `POST /control-center/today/action-envelope`; `POST /control-center/actions/{action_id}/approve`; `POST /control-center/actions/{action_id}/edit`; `POST /control-center/actions/{action_id}/reject`; `POST /control-center/actions/{action_id}/defer`; `POST /control-center/actions/{action_id}/local-task/commit`; `GET /control-center/actions/{action_id}/receipt`; `/api/runtime/invocations/{id}/approve`; `/api/runtime/invocations/{id}/execute`; dashboard provider-readiness read model plus `scripts/inspect_provider_credential_readiness.py`, `scripts/inspect_provider_credential_validation_lane.py`, `scripts/inspect_tiny_provider_invocation_lane.py`, `scripts/dev/uaa_runtime.py inspect-action-inbox-bridge`, `uaa runtime status`, `uaa runtime capabilities`, `uaa runtime invocations list/show`, `uaa runtime receipts show`, `uaa runtime safe-disable`, and `uaa actions approve|deny`. | Broad action execution contract, connector-write contracts, provider credential vault secret-resolution/runtime contract, broad provider validation outside the credential-validation capability, provider SDK/network adapter enablement outside named exact-scoped provider capabilities, and rollback execution verification beyond the local task and governed RuntimeGateway command safe-disable posture. | Backend decision state plus AuthorityLease-gated local capabilities only. Today-to-Action creation produces local receipt refs only after active `workspace/draft` AuthorityLease evaluation and still does not execute work; approve/edit/reject/defer produce local receipt refs without executing work. `local_task_create` can commit one approved Action Inbox item into local task state only after exact LocalApprovalAuthority validation, active `workspace/write` AuthorityLease evaluation, idempotency, receipt, evidence refs, and backend-owned safe-disable posture; if that posture is disabled or the active lease lacks Workspace write, the commit route denies before local task mutation. Governed Runtime Phase 07 can execute only the approved focused pytest, repo-verifier, frontend-check, and repo-doctor RuntimeGateway command capabilities with deterministic approval/envelope/scope/payload/policy refs, idempotency, redacted receipt/evidence refs, approved repo-root pinning, configured endpoint matching, receipt-detail truth, safe-disable posture, and CLI/Control Center/evidence timeline inspection parity; CLI approval requires a safe exact-envelope preflight before recording approval. The approval envelope, receipt visibility, provider-cost posture, credential-validation posture, provider invocation posture, and runtime bridge cards are bounded grammar; they do not send, run arbitrary commands, install, dispatch, write memory, collect secrets, perform broad provider validation outside the approved credential-validation capability, perform connector writes, call provider SDKs by default, run autonomous/background model calls, inject context, affect external systems, authorize billing, or grant production authority. Mock/degraded `/actions` fallback data is non-authoritative and cannot claim Python-core read-model ownership, backend eligibility, provider readiness, validation readiness, commit readiness, runtime execution readiness, committed receipt state, replay posture, or conflict posture. | `local_dev_workspace_only`; credential validation is `governed_network_read_only` and `mutating_requires_authority`; governed runtime command execution remains `mutating_requires_authority` through RuntimeGateway. | Read-only inbox, receipt, provider-readiness, and runtime bridge inspection need no approval. Today-to-Action creation requires active `workspace/draft` AuthorityLease scope, idempotency, authority decision refs, and receipt refs; approve validates exact LocalApprovalAuthority scope; edit, reject, and defer require idempotency, audit, and receipt refs. Local task commit requires active `workspace/write` AuthorityLease scope, approved `local_task_create` action kind, exact LocalApprovalAuthority scope, idempotency, backend safe-disable posture, and receipt refs. The runtime focused pytest, repo-verifier, frontend-check, and repo-doctor capabilities require deterministic Action Inbox approval/envelope/scope/payload/policy refs, idempotency, redacted command receipt refs, and safe-disable posture; `uaa actions approve|deny` accepts safe refs as lookup selectors only and does not treat approval refs as authority. The credential validation route requires active `provider_model_calls/execute` AuthorityLease scope, idempotency, exact approval, exact provider/policy/credential refs, revocation/safe-disable refs, and redacted receipt refs. Provider invocation capabilities require idempotency, exact approval, CostGovernor posture, max-approved USD, and redacted receipt refs, but the default adapter remains disabled/no-execution. | Action proposal refs, queue-state group refs/counts, backend `approval_envelope` and `receipt_visibility` read-model fields, runtime Action Inbox bridge refs, runtime command receipt/evidence refs, runtime evidence timeline refs, provider readiness/cost binding refs, credential validation refs, provider invocation refs, Today-to-Action envelope authority decision refs, Today-to-Action envelope receipt refs, UAA-P1-073 Action envelope contract refs, side-effect class, risk class, blocked-state labels, backend decision receipt refs, local task commit receipt refs, local task AuthorityLease decision refs, `local_task_created` Evidence Timeline event refs, receipt/audit/idempotency/rollback refs, safe-disable posture refs, expected receipt refs, CLI parity refs, and evidence refs only. | Operational maturity scorecard: Action Inbox remains rank 3 overall while `local_task_create` and focused pytest, repo-verifier, frontend-check, and repo-doctor RuntimeGateway capabilities are rank 5 local execution with receipt/evidence and safe-disable posture refs. Provider readiness/cost/credential-validation/provider invocation posture remains rank 2 metadata/proposal review unless a later accepted AuthorityLease-gated adapter enablement milestone grants broader exact authority. FCC-INBOX-001 and FCC-ACTION-002 claim no broader maturity rank promotion. Broader action execution remains pending. | Broad action execution, provider credential vault secret resolution, broad provider validation outside the approved credential-validation capability, provider SDK/network adapter enablement outside named exact-scoped provider capabilities, connector writes, arbitrary shell/subprocess work outside approved governed capabilities, memory writes, context injection, external side effects, rollback verification, and broader vertical-loop hardening remain future work. |
| Morning Briefing | `/briefing` renders bounded local briefing summaries with source-readiness posture, priorities, blockers, stale-state posture, evidence gaps, missing source-contract refs, and next safe action labels. The read-only source readiness metadata card now shares the dedicated backend-owned Source Readiness read model and renders status counts and supported states for `ready`, `blocked`, `missing`, `metadata_only`, `unavailable`, and `not_configured` without connector controls. | `GET /control-center/morning-briefing/summary` and `GET /control-center/sources/readiness`. | Email/calendar read-only contracts, briefing refresh contract, notification contract. | Local briefing skeleton and source-readiness read model only; no email/calendar access, connector reads/writes/runtime, account auth, background refresh, notification delivery, model/provider output authority, memory writes, or production authority. | `local_dev_workspace_only`. | None for read-only briefing summaries. Source reads, refresh, notifications, connector access, and memory writes remain unscoped. | Briefing refs, safe summaries, source-readiness labels/counts, missing email/calendar/notification contract refs, stale-state posture, evidence-gap summaries, evidence refs, and blocked states only. | Partial. Source readiness has a dedicated backend-owned read-only route and UI binding; connector contracts and maturity promotion remain blocked. | Integration contracts, redacted source evidence binding, notification delivery contract, and visual baseline capture remain future work. |
| CRM | `/crm` renders the CRM Local Command Center M2 cockpit from the backend-owned Python core read model. It shows relationship refs, follow-up queue refs, timeline refs, pipeline and opportunity refs, smart-list refs, report refs, local storage posture, import/export preview posture, deterministic proposal refs, authority blockers, and CLI refs with no external CRM write or send controls. | `GET /control-center/crm/summary`, `GET /control-center/crm/relationships`, `GET /control-center/crm/timeline`, `GET /control-center/crm/follow-ups`, `GET /control-center/crm/pipelines`, `GET /control-center/crm/smart-lists`, and `POST /control-center/crm/local-mutations`; CLI parity through `scripts/dev/uaa_crm.py`. | Connector read-lane contract, send/write authority contract, contact import commit contract, account sync contract, and visual baseline capture. | Backend-owned local read model plus exact local mutation receipt lane only. Local mutation changes local CRM state only after exact LocalApprovalAuthority validation, active `contacts/write` AuthorityLease evaluation, idempotency, receipts, and rollback/safe-disable posture. No connector runtime, connector write, external CRM write, account sync, contact import commit, send, calendar write, provider/model call, live web, browser automation, hidden context injection, background autonomy, public beta, public release, production readiness, or production authority. | Read routes are `local_dev_workspace_only`; the exact local mutation route is `mutating_requires_authority`; `/crm` UI selection/filter state remains presentation-only. | Read-only CRM inspection requires no approval. Local CRM mutation requires `X-UAA-Idempotency-Key` or `X-UAA-Idempotency-Ref`, active `contacts/write` AuthorityLease scope, exact `LocalApprovalAuthority` scope, safe refs, replay/conflict posture, and redacted receipt refs. Connector reads, sends, calendar writes, external CRM writes, account sync, provider/model use, live web, browser runtime, and production authority require later accepted exact-scoped milestones. | CRM M2 contract ref, route refs, CLI refs, storage refs, local mutation receipt refs, idempotency refs, approval scope refs, authority decision refs, authority lease refs, authority domain/capability refs, safe display labels, timeline/follow-up/pipeline/smart-list/report refs, import/export preview refs, blocked authority refs, and redaction refs only. | Partial backend-owned local CRM lane: useful for local relationship inspection and exact `contacts/write`-gated local state receipts, but connector/source runtime and external send/write authority remain blocked. | Connector read-lane unblock, sends/writes unblock, visual baseline capture, broader source-readiness binding, and later authority-specific connector/write gates. |
| Memory Review | `/memory` renders the FCC-MEM-001 backend-owned Memory Workbench over the storage-backed review queue, lifecycle receipts, reviewed recall-only records, L1/L2/L3 projection refs, context-pack proposal refs, quality states, why-shown refs, and health counts. FCC-MEM-022 adds safe-query hash refs, feedback receipts, epistemic roles, observation candidates, probe, contradiction previews, and HRR readiness with HRR disabled. | `GET /control-center/today/summary`, `GET /control-center/memory/review`, `GET /control-center/memory/workbench`, `GET /control-center/memory/search`, `POST /control-center/memory/review/manual-candidate`, `GET /control-center/memory/l1-index`, `GET /control-center/memory/l2-index`, `GET /control-center/memory/l3-index`, `GET /control-center/memory/context-packs`, `POST /control-center/memory/context-packs/{context_pack_ref}/action-proposal`, `POST /control-center/memory/feedback`, `GET /control-center/memory/observation-candidates`, `GET /control-center/memory/probe`, `GET /control-center/memory/contradictions`, `GET /control-center/memory/review/{candidate_ref}/receipt`, `GET /control-center/evidence/timeline`, `POST /control-center/memory/review/{candidate_ref}/accept`, `POST /control-center/memory/review/{candidate_ref}/correct`, `POST /control-center/memory/review/{candidate_ref}/reject`, `POST /control-center/memory/review/{candidate_ref}/defer`, `POST /control-center/memory/review/{candidate_ref}/merge`, `POST /control-center/memory/review/{candidate_ref}/supersede`, `POST /control-center/memory/review/{candidate_ref}/forget-request`. | Automatic/broad memory write policy beyond accept/correct recall-only records, retention/delete/export execution contract, external CRM write/account-sync contract, connector write contract, context-injection contract, semantic/vector search contract, HRR/algebraic retrieval milestone, and broad Phase 6 execution hook contract. | Backend-owned Memory Review decisions record safe receipt refs. Accept/correct create reviewed recall-only `LocalMemoryStore` records with safe refs after exact LocalApprovalAuthority validation, active `memory/write` AuthorityLease evaluation, idempotency, receipt, Evidence Timeline, and safe-disable posture. Read-only mode denies with readable Memory write authority requirements instead of opaque blocked-lane language. Reject, defer, merge, supersede, and forget-request preserve auditable posture without creating recall records, deleting, exporting, or silently rewriting memory. Manual intake creates a review candidate only. Feedback can update local trust/stale/conflict posture for reviewed recall only. L1/L2/L3/context-pack/observation/probe/contradiction lanes remain read-only/proposal-only; Phase 6.1 records exact-approved, active `memory/draft` AuthorityLease-gated internal Action proposal receipts from context-pack refs only. No source/memory truth authority, hidden context injection, prompt context writing, model/provider authority, embeddings/vector DB, HRR/algebraic retrieval, semantic search, background indexing, context-pack injection, connector writes, raw transcript/prompt/source display, background sync, memory delete/export execution, external CRM writes, account sync, action execution, public beta, or production authority. | `mutating_requires_authority` for accept/correct reviewed recall writes; read-only and proposal/receipt inspection remains `local_dev_workspace_only`. | Read-only review, workbench, search, receipt, L1/L2/L3/context-pack/observation/probe/contradiction inspection require no approval. Manual intake, feedback, and receipt-only lifecycle decisions require idempotency and backend receipts. Accept/correct reviewed recall writes require active `memory/write` AuthorityLease scope, exact backend-owned LocalApprovalAuthority validation, idempotency, receipt refs, and safe-disable posture. Merge/supersede require explicit refs. Retain, automatic write, delete, export, CRM sync, context-injection, action execution, HRR retrieval, and loop-binding mutation require later exact scoped memory contracts. | Memory candidate refs, safe summaries, provenance refs, source refs, evidence refs, lifecycle receipt refs, reviewed recall record refs, approval scope refs, authority decision refs, authority lease refs, authority domain/capability refs, safe-disable refs, rollback posture refs, workbench group counts, health counts, quality reason refs, why-shown refs, duplicate/conflict refs, L1/L2/L3/context-pack/observation/probe/contradiction refs, feedback receipt refs, HRR readiness refs, Phase 6.1 internal Action proposal refs, idempotency refs, replay/conflict posture, Evidence Timeline refs, authority boundary, blocked states, and next safe action labels only. | Operational maturity scorecard: Memory remains rank 3 overall while `reviewed_memory_recall_write` is a rank 5 local execution capability for accept/correct reviewed recall writes only under active `memory/write` AuthorityLease scope. FCC-MEM-001 and FCC-MEM-022 are implemented for local functional memory review/read-model behavior, while production/product-readiness claims remain blocked. | Automatic/broad memory writes, delete/export execution, semantic/vector search, HRR/algebraic retrieval, provider/model extraction, external CRM/account sync, connector writes, context injection, broad Phase 6 execution hooks, and public/production authority remain future work. |
| Setup Assistant | `/setup` shows the macOS-first setup preview using the read-only backend summary when available and the existing mock fallback otherwise. It renders backend-owned ready/missing/blocked diagnostics, the complete typed lifecycle state sequence, plan/status/install/verify/repair/stop/rollback/receipts posture, explicit rollback contract/execution/rehearsal/restore-proof truth, required future health proof, and backend-owned provider credential/CostGovernor posture as blockers only. | `GET /control-center/setup-assistant/summary`; `scripts/dev/uaa_setup_lifecycle.py`; dashboard provider-readiness read model plus `scripts/inspect_provider_credential_readiness.py`, `scripts/inspect_provider_credential_validation_lane.py`, and `scripts/inspect_tiny_provider_invocation_lane.py`. | Exact setup mutation AuthorityLease milestone, live install/process/health/repair/stop/rollback service, durable lifecycle receipt storage, native SwiftUI shell, provider credential vault secret-resolution/runtime route, broad provider validation outside the credential-validation capability, and provider SDK/network adapter enablement outside named exact-scoped provider adapters. | Typed lifecycle foundation, diagnostics, and dry-run approval-envelope contract only. Plan/status/receipt inspection is available; install/verify/repair/stop/rollback are `blocked_by_authority`. Rollback contract refs exist, but approval alone does not make execution available and no rehearsal or restore proof is claimed. No installer authority, live process probe/control, file or credential mutation, signed installer readiness, shell/subprocess execution, model download, LaunchAgent installation/load/start, background-service installation/load/start, provider SDK/network call by default, billing authority, public distribution, production readiness, or production authority. | `validation_only`; `/setup` remains an inspection UI with no lifecycle execution controls. | Dry-run lifecycle/envelope/provider-cost refs may name exact future approval scope refs for review only; approval refs and max-approved USD refs remain identifiers and grant no setup mutation or provider authority. | Typed state/operation/health/diagnostic contracts plus bounded previews, exact scope refs, receipt refs, rollback refs, idempotency refs, safe-disable refs, provider readiness refs, and denied side-effect flags. No durable live setup receipt is claimed. | Partial; lifecycle contract parity and Q07 diagnostics implemented, activation blocked. | Separate exact setup mutation authority, interruption/rollback rehearsal, bounded real setup-log source, native macOS visual QA, signed/distribution proof, credential vault runtime, provider adapter enablement outside named exact-scoped lanes, and any live setup action remain future work. |
| Chat Local Operator | `/chat` probes a redacted local turn through the governed local chat gateway, records a durable safe Chat receipt, records reviewable Actions/Plans handoff receipts, and can render Product Loop 009 Chat to loop handoff posture from a backend-owned `chat_to_loop_handoff_read_model` while showing contract ref, route, model/runtime/auth/tool-denial truth, safe evidence refs, blocked states, and denied authority posture. OpenWebUI remains the separate local/dev shell. | `GET /v1/models`, `POST /v1/chat/completions`, `GET /runtime/readiness`, `GET /runtime/capability-matrix`, `GET /control-center/today/summary`, `POST /control-center/chat/turns`, `GET /control-center/chat/turns/{turn_ref}/receipt`, `POST /control-center/chat/turns/{turn_ref}/handoff`. | Auth setup/status route, tools/functions/streaming denial summary route, Memory Review decision binding, and later governed execution workflow routes. | UAA-P1-074 local operator truth, FCC-V1-004 durable receipt/handoff refs, and Product Loop 009 Chat to loop handoff read-model posture only; model output is not truth, memory, approval evidence, or execution authority. Handoffs create reviewable proposals only, including reviewed memory-intake proposal posture for remember-this. OpenWebUI and Control Center output are not authority, and OpenWebUI state is not product state. | `/v1/*` and Founder Loop Chat receipt routes are `local_dev_workspace_only`; runtime routes are `validation_only`; `/chat` uses local UI state plus backend-owned receipt refs and fail-closed read models. | Local gateway must be explicitly enabled and bearer-authorized; Chat receipt and handoff routes require idempotency; no approval grant converts model output into authority, memory write, context injection, or execution. | UAA-P1-074 contract/schema/verifier/test refs, FCC-V1-004 receipt/verifier/test refs, Product Loop 009 Chat to loop handoff contract/verifier/test refs, safe turn evidence refs, tool-denial refs, handoff receipt refs, read-model outcome refs, blocked authority refs, P0-005 smoke harness refs, M167 evidence matrix refs, P0-015 checklist evidence refs, API gateway tests, and latency report refs. | Proofed route surface: first-party Chat local operator truth, durable Chat receipts, reviewable handoff receipts, Product Loop 009 Chat to loop handoff read-model posture, and FCC-V1-007 proof evidence are implemented for the exact `/chat` behavior; handoff execution, memory writes, context injection, and product-readiness claims remain blocked. | Reviewed local model evidence, tools/functions/streaming denial summary route, Memory Review decision binding, and later Code/loop integration. |
| Plans | `/operator-loop` exposes the UAA-P1-011 readable proof chain for task decomposition, one safe capability approval path, and receipt/audit/latency/rollback inspection. `/plans` remains accessible loading/error/empty/blocked/denied state copy only, while the Today spine now exposes UAA-P1-073 reviewable Action envelope metadata and UAA-P1-074 Chat proposal handoff refs for plan summaries. | `GET /task-decomposition/status`, `GET /task-decomposition/catalog`, `POST /task-decomposition/classify`, `POST /task-decomposition/decompose`, `POST /task-decomposition/plans/validate`, `POST /task-decomposition/approval-requests`, `GET /task-decomposition/approvals`, `POST /task-decomposition/approvals/grants/capture`, `POST /task-decomposition/approvals/revoke`, `GET /task-decomposition/audit`, `GET /task-decomposition/metrics`, `POST /task-decomposition/plans/execute`, `POST /task-decomposition/run`, `GET /control-center/today/summary`. | DAG/status summary route, durable run binding route, pause/resume/cancel status route, replay summary route, and broader product Plans workflow binding. | Local task-decomposition execution requires active `workspace/execute` AuthorityLease scope before registered local handlers run; high-risk or approval-bound nodes still require exact LocalApprovalAuthority grants. UAA-P1-073 envelope metadata and UAA-P1-074 Chat handoff refs are read-only and do not grant plan execution or approval capture. | `local_dev_workspace_only` plus explicit AuthorityLease evaluation for execution routes; `/plans` and `/operator-loop` are local UI state only. | Active `workspace/execute` AuthorityLease scope is required for local plan execution, and exact approval grant is still required for each approval-bound safe registered capability; no unscoped approval ref authority. UAA-P1-073 approval refs and UAA-P1-074 handoff refs remain identifiers only. | Task audit summaries, metrics, approval queue, safe task decomposition result envelopes, AuthorityLease decision refs, authority domain/capability refs, authority lease refs, UAA-P1-073 envelope refs, UAA-P1-074 Chat handoff refs, exact scope refs, expected receipt refs, rollback/safe-disable refs, and blocked authority refs. | Partial: UAA-P1-011 readable loop, AuthorityLease-gated local task execution, UAA-P1-073 Action envelope posture, and UAA-P1-074 Chat handoff posture are surfaced; broader Plans product loop remains partial. | Broader product Plans workflow binding, UAA-P1-030 route status manifest, pause/resume/cancel/replay routes, and future Founder Command Center IA work. |
| Models | `/models` now exposes accessible loading/error/empty/blocked/denied state copy only, while `GET /control-center/local-models/status` exposes backend-owned read-only inventory/gateway posture, optional Ollama and MLX-LM readiness posture, and review-only lifecycle proposal refs. Runtime panels still carry the implemented readiness/capability summaries. UAA-P1-064 adds read-only Python Agent Core inventory and CLI inspection only; UAA-P1-066 is now represented as Control Center read-only status with no activation control. Provider catalog, provider credential/CostGovernor posture, credential-validation posture and provider invocation posture are displayed as metadata/blockers only. | `GET /control-center/local-models/status`, `GET /v1/models`, `POST /models/route/preview`, `POST /model-runtime/manifests/validate`, `POST /model-runtime/requests/validate`, `POST /model-runtime/responses/validate`, `GET /runtime/readiness`, `GET /runtime/capability-matrix`; `GET /control-center/providers/setup-guide`; `POST /control-center/providers/exact-approved-lanes/tiny`; dashboard provider-readiness read model plus `scripts/inspect_provider_credential_readiness.py`, `scripts/inspect_provider_credential_validation_lane.py`, and `scripts/inspect_tiny_provider_invocation_lane.py`; CLI-only `uaa local-model status/list/inspect` for UAA-P1-064 inventory. | Current loaded-model receipt route, memory-fit planner route, GGUF selection/approval/readiness route, llama.cpp lifecycle status route, dry-run switch planner, approval-bound switch execution route, one-big-model guard status, identity-update receipt route, tuning recommendation route, rollback status route, provider credential vault secret-resolution/runtime contract, broad provider validation outside the credential-validation capability, and provider SDK/network adapter enablement outside named exact-scoped provider adapters. | M160-M167 local model capability family only; model/provider output is not production authority. UAA-P1-064 adds read-only inventory and CLI inspection only; UAA-P1-066 renders read-only backend-owned inventory/status state, including Ollama and MLX-LM readiness labels, and review-only proposal refs. Provider readiness/cost/provider invocation rows do not collect secrets, broadly validate providers, invoke provider SDKs by default, run autonomous/background model calls, or authorize billing. The credential-validation capability is separate, AuthorityLease-gated and exact-approval gated, and cannot invoke models. Lifecycle, switch receipts, identity updates, safe-disable, rollback, provider SDK/network adapter enablement outside named exact-scoped provider adapters, and later backend contracts remain future Python Agent Core authority. Control Center and OpenWebUI render state and request governed actions only. | `/v1/*` and the provider invocation route are `local_dev_workspace_only`; credential validation is `governed_network_read_only` and `mutating_requires_authority`; model-runtime, model-route, provider setup guide, and `GET /control-center/local-models/status` routes are `validation_only` or local read-only; `/models` owns presentation state only. | Approved GGUF/model refs and reviewed local runtime settings are required before live local use. Exact approval is required for any start, stop, switch, identity update, lifecycle mutation, provider credential validation capability request, provider invocation capability request, or paid provider use. Provider credential validation and provider invocation execution also require active `provider_model_calls/execute` AuthorityLease scope where required. Unknown paid cost blocks until explicit approval and CostGovernor refs/receipts are in scope; the default provider invocation adapter remains disabled/no-execution. | M167 evidence matrix, local E2E smoke harness, P0-015 checklist evidence refs, P0-016 tuning hardening test refs, P0-017 operational runbook refs, UAA-P1-062 scope doc, UAA-P1-064 read-only inventory scope doc, UAA-P1-066 read-only Control Center status scope doc, provider catalog/cost literacy refs, provider credential readiness/cost binding refs, credential validation capability refs, provider invocation posture refs, backend-owned local model status refs, UAA-P1-064 core/CLI test refs, route preview decisions, latency results, and future backend receipts for status, fit plans, lifecycle actions, identity updates, redacted logs/status, safe-disable, and rollback. | Operational maturity scorecard: Local Models is rank 2 proposal review for read-only status, provider-cost posture, exact credential-validation posture, provider invocation blocked posture, and lifecycle proposal refs only. | Reviewed local operational recovery evidence, reviewed hardware evidence, later lifecycle/status receipts, one-big-model enforcement, switch rollback proof, credential vault secret-resolution/runtime, provider SDK/network adapter enablement outside named exact-scoped provider adapters, and any start/stop/switch/download authority remain future work. |
| Approvals | `/approvals` uses `ApprovalQueuePanel` with the backend-owned run-attached approval queue projection, the nested unified approval review for durable runs, provider/tool contracts, connector delivery, and coworker handoffs, scoped approval summary, and clearly labeled preview-only legacy cards. The panel is read-only and does not offer approve, deny, revoke, resume, execute, connector-send, provider-call, worker, or scheduler controls. | `GET /control-center/approvals/summary`, `GET /control-center/approvals/queue`, `POST /approvals/requests/validate`, `POST /approvals/grants/validate`, `POST /approvals/validate`, `POST /approvals/receipts/validate`, `POST /task-decomposition/approval-requests`, `GET /task-decomposition/approvals`, `GET /task-decomposition/runs/{run_id}/approvals`, `POST /task-decomposition/approvals/grants/capture`, `POST /task-decomposition/approvals/revoke`. | CCC live approval capture/revoke UI routes, approval evidence summary route, approval expiry and replay status route. | Python Agent Core and LocalApprovalAuthority remain the only approval authority; run-attached and unified approval review refs are identifiers only and do not resume, start, dispatch, send, call providers/tools, schedule workers, or execute work. | Control Center and `/approvals/*` routes are `validation_only`; task-decomposition approval routes are `local_dev_workspace_only`. | Exact-scope grant capture or revoke through approved backend contracts only; read-only run-attached and unified approval review inspection require no approval and grant no authority. | Approval summary, run-attached approval queue refs, unified approval review refs, provider/tool contract refs, connector delivery refs, coworker handoff refs, validation decisions, task approval queue, grant/revoke records, audit summaries, and durable run event refs. | Partial. | Expiry/replay status, live approval capture/revoke UI binding, Proof Detail binding for approval review items, and any authority-bearing approval mutation surface remain future scoped. |
| Files | `/files` shows safe file refs. `/files/review` shows review packets; its review-only buttons update local UI state until they are bound to the backend capture route. | `POST /files/refs/validate`, `POST /files/review/approvals/capture`, `POST /files/tree/preview`, `POST /files/read/preview`, `POST /files/write/propose`, `POST /files/diff/preview`. | Patch apply route, rollback receipt route, file operation status route, CCC summary binding to approval capture receipts. | Safe-root refs and server-owned file refs only; no raw file browsing or shell execution. File read/tree preview now requires active `files/read` AuthorityLease scope, write proposal/diff preview requires active `files/prepare`, and review approval capture persists safe refs only after active `files/write`. | `local_dev_workspace_only` plus explicit AuthorityLease evaluation for read/prepare preview routes; `mutating_requires_authority` for review-only approval capture. | Read/tree preview requires active `files/read`; write proposal/diff preview requires active `files/prepare`; review-only approval capture requires active `files/write`, idempotency, audit refs, receipt refs, redaction, rollback/safe-disable posture, and safe refs only. Patch apply, rollback execution, raw reads, context injection, memory writes, export, shell execution, and browser/app control remain future scoped authority work. | Safe tree refs, redacted preview result, read/prepare authority decision refs, review approval capture decision, authority lease refs, write proposal decision, diff summary, future rollback receipt. | Partial local file-domain authority: AuthorityLease-gated safe preview/proposal surfaces plus AuthorityLease-gated review-only safe-ref capture; no actual file mutation/apply workflow. | Backend File Review summary route, CCC binding to capture receipts, M173 atomic apply/rollback gates, and exact patch apply/test command authority remain future scoped. |
| Work Board | `/work-board` renders the backend-owned Work Board Kanban cockpit with card, column, local task-record, proof, evidence, blocker, promotion-path, drag/drop posture, exact approved reorder/local-card-create/local-task-record posture, active authority-domain requirements, and CLI inspection refs. Drag/drop, keyboard moves, filters, local draft, list view, and proof view remain presentation/local-preview until the operator persists exact approved changes through Python Core under an active `workspace/write` AuthorityLease. | `GET /control-center/work-board`, `POST /control-center/work-board/reorder`, `POST /control-center/work-board/cards`, `POST /control-center/work-board/tasks`; `scripts/dev/uaa_work_board.py inspect-board`, `scripts/dev/uaa_work_board.py inspect-reorder-receipt`, `scripts/dev/uaa_work_board.py inspect-card-create-receipt`, `scripts/dev/uaa_work_board.py inspect-task-create-receipt`. | Card archive/assignment route, issue tracker sync route, connector write route, assignment/milestone mutation route, task execution route, and rollback execution route. | Backend-owned Kanban read model plus exact local persisted reorder, local-card-create, and local-task-record receipts only; persisted mutation requires `Ask before changes` or stronger with `workspace/write` and exact approval. No card archive, task execution, assignment mutation, issue tracker write, connector write, provider/model call, shell/browser execution, background autonomy, raw path/content persistence, public release, or production authority. | `mutating_requires_authority` for persisted reorder, local card create, and local task-record create; read-only inspection remains `local_dev_workspace_only`. | None for read-only Work Board inspection or local preview. Persisted reorder, local card create, and local task-record create require active `workspace/write` AuthorityLease scope, exact scoped approval, idempotency, receipts, rollback/safe-disable posture, redaction, CLI parity, and verifier coverage. Future card archive/assignment, task execution, issue tracker sync, connector writes, and rollback execution require separate domain/capability lease support plus exact scoped implementation. | Work Board refs, column refs, card refs, local task refs, proof refs, evidence refs, blocked authority refs, authority decision refs, authority lease refs, drag/drop posture refs, reorder/local-card-create/local-task-create receipt refs, promotion-path refs, CLI refs, and local preview notices only. | Partial backend-owned read model with AuthorityLease-gated exact approved reorder, local-card-create, and local-task-record persistence. | Issue tracker sync, task execution, card archive/assignment, assignment/milestone mutation, connector writes, and rollback execution remain future scoped. |
| Coding Cockpit | `/coding` renders the repo-safe Coding Cockpit shell with workspace/context, task timeline, diff/proof, chat, terminal preview, Git preview, test output, live preview, authority mode posture, context-pack preview, proposal-only patch preview, blocked patch-apply readiness, approval-required RuntimeGateway validation command readiness, blocked Git review, blocked live-preview readiness, blocked multi-agent review readiness, and the Pair Agents preview/readiness panel from backend-owned read models. | `GET /control-center/coding/session`, `GET /control-center/coding/context`, `GET /control-center/coding/patch-proposal`, `GET /control-center/coding/patch-apply-readiness`, `GET /control-center/coding/test-command-readiness`, `GET /control-center/coding/git-review`, `GET /control-center/coding/live-preview`, `GET /control-center/coding/multi-agent-review`; `scripts/dev/uaa_coding.py inspect-session`, `scripts/dev/uaa_coding.py inspect-context`, `scripts/dev/uaa_coding.py inspect-patch-proposal`, `scripts/dev/uaa_coding.py inspect-patch-apply-readiness`, `scripts/dev/uaa_coding.py inspect-test-command-readiness`, `scripts/dev/uaa_coding.py inspect-git-review`, `scripts/dev/uaa_coding.py inspect-live-preview`, `scripts/dev/uaa_coding.py inspect-multi-agent-review`, `scripts/dev/uaa_coding.py inspect-pair-agent-relay`, `scripts/dev/uaa_coding.py preview-pair-run`, `scripts/dev/uaa_coding.py inspect-pair-run`, `scripts/dev/uaa_coding.py inspect-pair-artifacts`, `scripts/dev/uaa_coding.py inspect-pair-receipts`, `scripts/dev/uaa_coding.py start-pair-run-readiness`, and `scripts/dev/uaa_coding.py stop-pair-run-readiness`. | Exact patch apply execution route, Coding-specific command receipt drilldown, live Git status/diff reader, approved Git stage/commit/push/PR routes, live preview execution route, multi-agent execution lane, foreground paired-agent relay runner, and universal Proof expansion. | Read-only session, context-pack preview, patch proposal inspection, apply-readiness blocker inspection, approval-required validation command readiness inspection backed by RuntimeGateway, Git review inspection, live-preview readiness inspection, multi-agent review readiness inspection, and Pair Agents preview/readiness inspection only; no file writes, repo file reads, patch apply, command execution from the Coding route, arbitrary shell/subprocess execution, live Git command execution, Git mutation, dev server start or status detection, raw URL persistence, screenshot capture, console capture, provider/model calls, provider SDK calls, local agent execution, paired-agent foreground adapter execution, generic agent bus, multi-agent execution, context injection, raw prompt/response/transcript persistence, browser automation, connector writes, background autonomy, raw path/content persistence, public release, or production authority. | `local_dev_workspace_only`. | None for read-only session, context preview, patch proposal inspection, apply-readiness inspection, validation command readiness inspection, Git review inspection, live-preview readiness inspection, multi-agent review readiness inspection, or Pair Agents preview/readiness inspection. Exact RuntimeGateway validation command execution still requires active lease scope, exact Action Inbox approval, idempotency, receipts, safe-disable posture, and redaction through `POST /api/runtime/invocations/{id}/execute`. Future patch apply, expanded command execution, live Git read, Git mutation, browser, dev server, provider, local-agent, paired-agent foreground relay, connector, or production lanes require exact scoped authority, receipts, rollback/safe-disable posture, redaction, CLI parity, and verifier coverage. | Coding session refs, workspace/context refs, task refs, context-pack refs, context comparison refs, budget refs, patch proposal refs, hunk refs, apply-readiness refs, validation command readiness refs, runtime lane refs, runtime execution route refs, runtime CLI refs, allowlist refs, expected receipt refs, rollback refs, command proposal refs, Git review refs, Git status refs, Git diff refs, changed-file refs, commit proposal refs, PR description proposal refs, live-preview refs, dev-server status refs, preview URL refs, screenshot refs, console refs, visual-proof refs, route-checklist refs, viewport refs, multi-agent review refs, Pair Agents run/slot/state/turn/artifact/receipt/proof/evidence refs, agent slot refs, plan/review/diff-comparison/disagreement/handoff refs, proof refs, blocked authority refs, redaction refs, promotion-path refs, unblock-prompt refs, and next-safe-action refs only. | Partial repo-safe Prompt 08 multi-agent review readiness plus RuntimeGateway-backed validation command readiness and Coding Pair Agent Relay Runner preview/readiness. | Approved apply execution, expanded allowlisted test command execution, live Git read/mutation, live preview execution, provider review, local-agent verifier, foreground paired-agent relay execution, and multi-agent execution lanes remain future scoped. |
| Runtime | `/runtime`, `/runtime/local`, `/runtime/manual-smoke`, `/storage`, dashboard summaries, Foundation Gate panel, API route inventory, and Governed Product Pilot authority-profile inspection. | `GET /health`, `GET /version`, `GET /api/manifest`, `GET /runtime/readiness`, `GET /runtime/capability-matrix`, `POST /runtime/smoke-reports/validate`, `GET /api/runtime/governed-product-pilot-profile`, `GET /control-center/status`, `GET /control-center/runtime-readiness/summary`, `GET /control-center/foundation-gate/summary`, `GET /control-center/storage/status`, `GET /control-center/routes`; CLI parity through `scripts/dev/uaa_runtime.py authority-profile`. | Local model readiness aggregate route, latency report route, queue status route, installed/current model status route, memory-fit status route, loopback llama.cpp reviewed-settings lifecycle status route, redacted lifecycle log/status route. | Read-only/validation-only runtime status and exact Governed Product Pilot profile posture until a scoped milestone grants exact local lifecycle authority. UAA-P1-062 shaped status and planning first; RuntimeGateway pilot capabilities may expose only exact local model, command, portable evidence, and durable run posture with receipts and blocked authority refs. Direct RuntimeInvocationStore, CLI, and API paths now evaluate active/default AuthorityLease scope: read-only status commands require `workspace/read`; execution-capable focused pytest, repo-verifier, frontend-check, repo-doctor, and exact guarded Hermes CLI chat capabilities require active `workspace/execute`; configured local loopback model calls require Full Machine Access plus active `provider_model_calls/execute` before transport execution; Action Inbox approval is still required where applicable and command execution refreshes the active lease decision immediately before process start. Start/stop/switch remains blocked until exact backend authority, approvals, receipts, and rollback are accepted. | `/health`, `/version`, and `/api/manifest` are `none`; runtime and Control Center summary routes are `validation_only`; `/api/runtime/governed-product-pilot-profile` is protected local-sensitive read-model truth; Founder Loop storage status is `local_dev_workspace_only`. | No approval for read-only status or authority-profile inspection. Execution-capable command pilot capabilities require active `workspace/execute` AuthorityLease scope plus exact Action Inbox approval where applicable; execution rechecks active lease scope before process start and blocks if the lease is stale, expired, revoked, or missing. Hermes chat also requires exact guarded argv, query hashing, redacted receipts, and interface-mode opt-in before any external CLI execution. Local loopback model calls require active `provider_model_calls/execute` AuthorityLease scope and remain loopback/configured-endpoint only, metadata-only, and non-authoritative. Any lifecycle launch, stop, switch, or identity update must be separately scoped and approved. | Runtime readiness report, Governed Product Pilot profile refs, portable evidence envelope refs, durable orchestration posture refs, authority decision refs, authority lease refs, capability matrix, Foundation Gate summary, Founder Loop storage refs, P0-015 checklist evidence refs, performance report refs, UAA-P1-062 scope doc, and future status/fit/lifecycle receipt refs. | Partial governed product pilot profile plus exact AuthorityLease-gated RuntimeGateway pilot capabilities. | Reviewed local llama.cpp lifecycle prerequisite evidence, UAA-P1-013 verification lanes, UAA-P1-030 route status manifest, broader lifecycle CLI/API parity, and redacted local model manager receipts. |
| Evidence | `/evidence` starts with the storage-backed UAA-P1-069 Evidence History Grammar timeline, including UAA-P1-074 Chat local operator, UAA-P1-075 governed Code proposal history, UAA-P1-076 cross-surface memory intake proposal history, UAA-P1-077 memory-to-loop binding history, UAA-P1-078 private beta-readiness gate history, and UAA-P1-079 user intent proposal history, then keeps the existing evidence viewer. Product Loop 010 adds the fail-closed Evidence Timeline narrative read model over existing safe refs. Run Observability adds a backend-owned read model for durable run lifecycle, progress, approval, coworker, connector delivery, receipt, evidence, proof, and blocked authority refs. `/receipts`, `/events`, `/events/timeline`, `/foundation-gate`, and `/api-routes` show redacted summaries and refs. | `GET /control-center/today/summary`, `GET /control-center/evidence/timeline`, `GET /control-center/runs/observability`, `POST /receipts/preview`, `POST /events/validate`, `GET /task-decomposition/audit`, `GET /task-decomposition/metrics`, `GET /observability/session-events`, `POST /observability/client-errors`, `GET /control-center/foundation-gate/summary`, `GET /control-center/routes`, `GET /web-evidence/status`, `POST /web-evidence/request`, `POST /control-center/web-evidence/attach`. | Release evidence index route, latency report summary route, rollback status route, governed evidence display binding, and broader observability dashboard work. | Evidence is read-only review material; safe refs, redacted summaries, Product Loop 010 Evidence Timeline narrative text, and run observability refs only. Timeline refs, narrative refs, approval refs, connector delivery refs, coworker refs, and proof refs are identifiers and do not grant approval, perform rollback, enable connector runtime, send connector deliveries, stream live progress, start background workers, apply code changes, write memory, inject context, execute memory-derived actions, execute intent, call providers/models, open beta access, or confer production authority. | Ledger and Control Center evidence routes are `validation_only`; task-decomposition audit/metrics, observability summaries, and Founder Loop timeline summaries are `local_dev_workspace_only`; governed web evidence request and Control Center web evidence attach are `governed_network_read_only`. | No approval for read-only evidence inspection. Web evidence attach requires active `browser/read` AuthorityLease scope plus configured host allowlist before WebAccessGateway fetch; other mutating evidence generation must follow its owner route. | Evidence Timeline entries, the Product Loop 010 Evidence Timeline narrative, and Run Observability show readable history answers, what happened, why recorded, approval posture, durable lifecycle refs, progress refs, coworker handoff refs, connector delivery refs, proof refs, blocked runtime states, what changed, what remains blocked, inspection refs, receipt, audit, replay, rollback posture, latency, Foundation Gate, source-readiness, governed Code validation/receipt posture, cross-surface memory intake proposal posture, memory-to-loop binding posture, private beta-readiness criteria, user intent confidence/ambiguity/routing posture, redaction, stale-state, missing-evidence, blocker, and next-safe-action posture; existing evidence views remain redacted summaries and refs. | Proofed route surface: UAA-P1-069 grammar, UAA-P1-074 Chat evidence, UAA-P1-075 governed Code evidence, UAA-P1-076 memory intake evidence, UAA-P1-077 memory-to-loop evidence, UAA-P1-078 private beta-readiness evidence, UAA-P1-079 user intent evidence, FCC-V1-006 Evidence Timeline productization, Product Loop 010 Evidence Timeline narrative, FCC-V1-007 proof evidence, and Run Observability read-only inspection are implemented for the exact `/evidence` behavior; richer release evidence remains pending. | Dedicated evidence index routes, richer observability dashboard work, and route status manifest follow-up bindings. |
| Settings | `/settings` now has backend-owned read-only status from `GET /control-center/settings/status` for maturity gate status, feature-flag posture, kill-switch posture, route safety refs, blocked authority classes, review-only settings proposal refs, and Product Loop 011 authority posture labels. The posture matrix covers web, providers, connectors, memory context use, model runtime, local model lifecycle, and platform capabilities with blocked/degraded/partial/metadata-only states. `/settings` also exposes AuthorityLease mode controls for implemented local domains, active lease and receipt issued/expires visibility, a decision-preview cockpit for concrete workspace command, file proposal, browser click, and budgeted-purchase requests, and a mission-planner cockpit for delegated AuthorityLease issue requirements. Provider credential readiness, CostGovernor binding posture, credential-validation posture and provider invocation posture are also rendered from backend dashboard data as blockers only. Local backend base policy remains code/config only, and FCC-P1-011 remains the Settings kill-switch/feature-flag spec foundation. | `GET /control-center/settings/status`, `GET /api/runtime/authority-state`, `POST /api/runtime/authority-decisions/preview`, `POST /api/runtime/authority-missions/plan`, `POST /api/runtime/authority-leases`, `POST /api/runtime/authority-leases/approve-and-issue`, `POST /api/runtime/authority-leases/revoke`, plus related status from `/control-center/status`, `/control-center/runtime-readiness/summary`, `/control-center/local-models/status`, `/control-center/sources/readiness`, `/api/manifest`, dashboard provider-readiness read model, `scripts/dev/uaa_runtime.py inspect-authority-state --json`, `scripts/dev/uaa_runtime.py preview-authority-decision --json`, `scripts/dev/uaa_runtime.py plan-authority-mission --json`, `scripts/inspect_settings_authority_posture.py`, `scripts/inspect_provider_credential_readiness.py`, `scripts/inspect_provider_credential_validation_lane.py`, and `scripts/inspect_tiny_provider_invocation_lane.py`. | Loopback auth-token setup/status route, reviewed local llama.cpp settings route, local model identity/alias status route, local model lifecycle safe-disable route, provider configuration route, credential vault secret-resolution/runtime route, broad provider validation route outside the approved credential-validation capability, provider SDK/network adapter enablement outside named exact-scoped provider adapters, kill-switch execution route, and any mutation route beyond approved capabilities. | Settings must remain disabled-by-default, redacted, local-only, revocable, and policy-gated. Feature-flag, kill-switch, permission-mode, model identity, lifecycle, provider, provider-cost, connector, web, memory-context, and platform names are posture vocabulary only until separate scoped authority exists. Decision and mission previews show required mode, domain/capability refs, blocked/degraded reasons, unsupported adapter refs, lease-issue readiness, receipt/audit refs, and no-execution/no-mutation posture; they do not open a browser, charge payments, call providers, run commands, mutate files, issue leases, or grant unsupported adapters. No toggles that grant authority, provider configuration, secret entry, provider credential validation outside the approved credential-validation capability, installer behavior, runtime activation, feature-flag writes, kill-switch execution, revocation execution, connector runtime, connector writes, live web, autonomous/background model calls, provider SDK/network calls by default, shell/browser execution, public beta, production readiness claims, or production authority is added. | `GET /control-center/settings/status`, `POST /api/runtime/authority-decisions/preview`, and `POST /api/runtime/authority-missions/plan` are `validation_only`; `/settings` owns presentation state only and renders backend-owned posture rows, lease receipts, decision previews, and mission plans. | No approval is required for read-only Settings, provider-readiness status, authority decision preview, or authority mission planning. Exact approval is required for any setting that enables runtime authority, persistence, local lifecycle behavior, model identity mutation, provider configuration, provider credential validation capability request, provider invocation capability request, paid provider use, kill-switch execution, revocation execution, or other mutation. Provider credential validation and provider invocation execution also require active `provider_model_calls/execute` AuthorityLease scope where required; default provider invocation route remains disabled/no-execution. | FCC-P1-011 spec refs in `docs/control_center/SETTINGS_KILL_SWITCH_FEATURE_FLAGS_SPEC.md`, Product Loop 011 docs/verifier/CLI refs, AuthorityLease state, decision preview, mission-plan, and lease receipt refs, provider credential readiness/cost binding refs, credential validation capability refs, provider invocation posture refs, operational maturity manifest/verifier refs, route-status refs, blocked authority refs, platform capability snapshot refs, and safe settings proposal refs only; future local model manager/provider receipts must use redacted refs only. | Operational maturity scorecard: Settings is rank 3 authority cockpit for backend-owned status, mode controls, revoke receipts, decision previews, mission-plan previews, blocked/degraded/partial posture labels, provider-cost/exact-credential-validation/provider invocation posture, and review-only settings proposal refs only. | Mutating feature flags, kill-switch execution, revocation execution, provider configuration, credential vault secret-resolution/runtime, broad provider validation outside the credential-validation capability, provider SDK/network adapter enablement outside named exact-scoped provider adapters, reviewed runtime settings mutation, local model identity mutation, and lifecycle safe-disable execution remain future scoped milestones. |

Settings mission-planner note: issue-ready mission plans may call
`POST /api/runtime/authority-leases/approve-and-issue` with the backend-generated
`lease_issue_request` to issue a mission-scoped lease for implemented local
domains only. The lower lease route accepts an approval ref only after the
backend has durably captured the exact current approval; no Control Center
payload may supply grant data. Mission-scoped leases apply only to later action
requests carrying the matching safe mission ref in resource refs or constraints.
Draft-only mission plans with denied domains or unsupported adapters must keep
the issue control disabled and must not claim browser, payment, connector,
provider, shell, or production execution.

Patch Workbench Phase A remains contract/readiness work only. Phase B apply
authority is blocked until an exact apply route, CLI/API/core parity,
backend-owned durable receipts, Evidence Timeline events, rollback or
safe-disable verification, and redacted secret-like diff blocking are all
implemented and verifier-backed.

## Visible Action Map

Current visible actions expose UAA-P1-011 as inspection evidence only:

| Visible action | Current behavior | Route authority and side-effect class | Product-readiness result |
|---|---|---|---|
| Navigate/select a card or row | Local UI state only. | No backend route. | Safe for review, not completion evidence. |
| Preview action | Calls `POST /control-center/actions/preview`. | `validation_only`; execution remains denied. | Safe preview only. |
| Record Action Inbox decision | Calls `POST /control-center/actions/{action_id}/approve`, `/edit`, `/reject`, or `/defer`, then reads `GET /control-center/actions/{action_id}/receipt`. | `local_dev_workspace_only`; decision state is backend-owned and mutating routes require local auth, exact approval where required, idempotency, and receipts. | Records decision receipt refs only; action execution remains denied. |
| Commit approved local task | Calls `POST /control-center/actions/{action_id}/local-task/commit` only for eligible approved `local_task_create` items with backend-owned envelope, receipt visibility, active `workspace/write` AuthorityLease scope, and enabled safe-disable posture. Mock/degraded data cannot expose this control. | `local_dev_workspace_only`; active `workspace/write` AuthorityLease, exact LocalApprovalAuthority scope, idempotency, durable local receipt, Evidence Timeline event, CLI parity, and safe-disable posture are required. | Creates local task state only; connector writes, shell/subprocess execution, model/provider authority, memory write, context injection, external side effects, rollback execution, broad action execution, and production authority remain denied. |
| Inspect File Review approval posture | Displays backend/mock safe-ref posture only. The web panel exposes no Approve/Deny control and never synthesizes persisted state. | No backend route call in CCC Web; the separately governed exact capture route remains unwired here. | Inspection only; not approval or completion evidence. |
| Load dashboard/runtime/routes summaries | Reads local summary endpoints. | `none` or `validation_only`. | Safe status evidence only. |

No visible CCC action currently sends a chat message, launches llama.cpp,
selects a GGUF model, creates a broader product Plans loop, executes a
generic or external Action Inbox decision, executes a
capability outside the existing approval-bound backend path, applies a file
patch, rolls back a mutation, changes Settings, or grants broad authority.

Future visible actions for Today, Inbox, Plans, Actions, Memory, Evidence, and
Settings must document the backing Python core/API contract, side-effect class,
approval requirement, command-line or repo-local script inspection path, tests,
and redacted evidence before they can be treated as operator-relevant product
behavior. A React-only implementation is local presentation state, not product
workflow completion evidence.

The bounded FCC-V1 conveyor in
`docs/control_center/FOUNDER_LOOP_V1_MILESTONES.md` is complete through
FCC-V1-007 for converting posture into one receipt-bearing Founder loop.
`/actions`, `/chat`, `/memory`, and `/evidence` are proofed only for their
exact backend-owned route-surface behavior; `/today`, `/inbox`, `/settings`,
model lifecycle, action execution, connector workflows, and product-readiness
claims remain partial, blocked, or future-scoped.

## First Product Loop Gaps

UAA-P1-011 now has a readable Control Center proof chain for runtime health,
local model readiness, UAA `/v1` chat readiness, task plan creation, one safe
capability approval path, and receipt/audit/latency/rollback inspection. The
remaining gaps before broader product-readiness claims are:

1. FCC-LOOP-001 now implements the first UI/readability collapse around core
   surfaces. Today, Inbox, Plans, Actions, Memory, Evidence, and Settings share
   a compact daily-loop spine while route authority, safe refs, and blocked
   states stay visible. This did not add backend routes, connector runtime,
   storage authority, or operational maturity promotion.
2. Use the UAA-P1-030 route status manifest as release evidence for visible
   action owners, auth posture, side-effect class, risk class, release status,
   OpenAPI operation ids, approval requirements, and evidence refs.
3. Add installed GGUF, current loaded-model, and reviewed GGUF
   selection/approval/readiness routes using safe refs only.
4. Add reviewed loopback llama.cpp settings, lifecycle status, redacted
   logs/status, and UAA-P1-062 memory-fit/switch planning before any lifecycle
   execute controls.
5. Extend the CCC Chat Local Operator that uses UAA `/v1` with local bearer status, auth
   failure handling, safe failure handling, and visible tools/functions/
   streaming denial into durable receipt and governed handoff workflows.
6. Add a CCC Plans surface over classify, decompose, approval request, grant
   capture, safe registered capability execution, audit, and metrics.
7. Continue expanding Evidence views beyond the FCC-P1-006 timeline only after
   durable run, evidence-index, rollback-status, and latency-report contracts
   are scoped.
8. Treat UAA-P1-086 as the completed API boundary enforcement-test checkpoint
   before any authority-heavy Plans, Chat, Code, loop-binding, or private
   beta-readiness claims can advance on later workflow evidence.
9. Keep the completed FCC-V1 Founder Loop V1 conveyor honest: FCC-V1-000 through
   FCC-V1-007 are complete for release-surface truth, API perimeter posture,
   Action decisions, first Today-to-Action receipt loop, Chat durable receipts
   and handoffs, Memory Review decisions, Evidence Timeline productization, and
   exact proofed route-surface promotion of `/actions`, `/chat`, `/memory`, and
   `/evidence`; broader product readiness remains blocked until later evidence.

These remaining gaps are release blockers for M172 product-readiness claims.

## Rollback

Rollback refs are inspection evidence only in this map. Any future Control
Center rollback action must name the exact backend route, approval boundary,
receipt ref, audit ref, idempotency key, and no-raw-content evidence before it
can be presented as a product-ready recovery path.

## Product Language Rules

The canonical enforceable UAA-P1-031 rules live in
`docs/control_center/PRODUCT_LANGUAGE_RULES.md`.

- No hidden authority: visible Control Center actions must name their backend
  authority boundary and side-effect class.
- No frontend-only product behavior: visible operator workflows must preserve a
  CLI/core/API path and cannot exist only in React state.
- No fake completion: preview-only or blocked flows must not be described as
  completed product work.
- No raw JSON as primary UI for operator-critical flows: operator surfaces need
  readable summaries, states, and evidence refs before claiming readiness.

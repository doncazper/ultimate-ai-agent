# Mobile Device Manifest Schema TODO

Status: Future planning placeholder only, v0.14.4

This is not a JSON schema and is not a runtime contract. It records future schema intent for a later reviewed milestone.

Future schema concept:

```text
MobileDeviceIdentity
```

Future fields:

```text
device_id
owner_user_id
platform
app_instance_id
trust_status
pairing_status
last_seen_at
capabilities_declared
revocation_status
```

Non-goals for v0.14.4:

```text
no mobile app
no device pairing
no runtime registry
no OS permission integration
no sensor access
no background service
```

# Ultimate AI Agent Workspace Standards

Active baseline: v0.43.0.

This repository is the Ultimate AI Agent foundation workspace. Treat it as a contract-first Python core, not a production runtime integration layer.

## API Boundary

- `/api/manifest` is the typed metadata endpoint for the current API boundary.
- OpenAPI is the public route contract. Keep operation IDs stable, unique, and generated from method plus path.
- API routes may validate, preview, evaluate policy, or expose metadata. They must not perform production provider calls or autonomous external actions.
- Do not add runtime model calls.
- Do not add web fetching.
- Do not add provider SDK calls, browser automation, production persistence, scanner runtimes, or runtime agent config loading.
- M8 model runtime endpoints are simulated/dry-run only. Do not add live runtime URLs, tokenizers, billing APIs, network calls, or provider SDK calls.
- M8.5 approval endpoints are local/dev validation-only. Do not treat arbitrary approval strings as authority, and do not add production auth, OAuth, persistence, or external actions.
- M9 local loopback runtime support is dev-only, loopback-only, approval-gated, and must default to validation or simulated fallback. Caller policy cannot disable the loopback-only guard or use allowed host lists to authorize remote hosts; hostile policy inputs should fail validation before adapter execution. Tests and Foundation Gate must use fake transport and must not make real network/model calls.
- M10 manual local loopback smoke support is manual-only, disabled by default, approval-gated, loopback-only, and restricted to a fixed non-sensitive smoke prompt. Do not pass user prompts, files, memory, context packs, secrets, or task content into smoke execution. Do not add a public smoke execute API route.
- M10.5 remote worker support is foundation-only, disabled by default, mock/local metadata only, and dry-run only. Do not add live mesh networking, tailnet execution, listeners, network calls, job dispatch, remote subagents, remote Tool Broker execution, remote approvals, personal-data access, write/send actions, critical remote work, background services, or private transport configuration.
- v0.14.3 keeps private mesh/tailnet terms vendor-neutral and open-source-first. Headscale, generic WireGuard, Tailscale, private mesh, tailnet, and LAN transports are planned/disabled metadata only. Do not install, call, configure, or integrate Headscale, Tailscale, tailscaled, WireGuard, or `wg`; do not commit tailnet names, hostnames, private IPs, node keys, auth keys, OAuth data, credentials, or tokens.
- v0.14.4 mobile companion work is planning-only. Phone/mobile is a future control, approval, capture, receipt, and status surface, not the agent brain. Do not add a mobile app, iOS/Android/native package, React Native, Expo, Flutter, Swift, Kotlin, Capacitor, Ionic, sensor access, OS permission integration, device pairing, background service, notification runtime, network call, autonomous mobile action, or runtime Device Capability Broker.
- v0.14.5 documentation integrity work may update docs, release notes, verifier scripts, and gate documentation checks only. Keep active docs aligned with `VERSION.md`, `pyproject.toml`, package `__version__`, README import docs, release notes, API docs, roadmap, and Foundation Gate plan.
- v0.14.6 Codex plugin governance work is docs/policy-only. Do not enable plugins, install plugins, configure plugins, add plugin marketplace/install behavior, run iOS/macOS builds, run Xcode, run simulators, access keychains/signing identities/provisioning profiles/App Store Connect, use Chrome authenticated profile control, use Computer Use automation, run Hugging Face jobs/uploads/training, or add dependencies/runtime capability. Browser + Build Web Apps are the future Web Control Center boundary with approval only. CodeRabbit/GitHub read-only review may be used for release readiness with explicit review prompts; GitHub write/release actions require direct approval or direct-push rules.
- v0.15.0 runtime readiness work is report/validation/status-only. Do not turn readiness reports, capability matrix entries, manual smoke reports, or runtime API status routes into execution authority. Do not add execute/run/connect/dispatch/provider/plugin-enable/mobile-sensor/native-build routes. Manual smoke reports are not production readiness evidence, and model output is not truth authority.
- v0.15.1 is a taxonomy clarification patch only. `local_loopback_policy` means supported validation-only policy, not runtime execution readiness. `fake_manual_loopback_smoke` means fake/test report origin only, not production smoke evidence.
- v0.16.0 Control Center work is backend contract/API only. Action preview is not execution.
- v0.17.0 Web Control Center work allows the local React/Vite shell under `apps/control-center` only. Do not add backend execution routes, production Control Center authority, model/provider calls, remote dispatch, mobile/native code, sensor access, plugin enablement, Chrome authenticated profile control, Computer Use automation, iOS/macOS build workflow, analytics/auth/payment/SaaS SDKs, or production persistence.
- v0.17.1 Web Control Center safety polish keeps the shell read-only/preview-only. Do not add execute/run/send/deploy/enable/approve action controls, arbitrary frontend endpoint construction, sensitive browser storage, cookies, credential APIs, camera/microphone/location/notification/push APIs, secret-like mock fixtures, or production Control Center authority.
- v0.17.2 Web Control Center verification hardening keeps browser smoke readiness manual, local-only, unauthenticated-profile-free, and non-authoritative. CI may run npm install/typecheck/lint/test/build inside `apps/control-center`; it must not run browser automation, Chrome authenticated profile control, Computer Use, external smoke targets, deployment, native/iOS/macOS builds, plugin enablement, mobile sensor access, or runtime/model/provider execution.
- v0.17.3 documentation current-release label cleanup keeps active docs, release notes, import docs, roadmap, and documentation integrity checks aligned. Do not use this docs-only patch to add runtime/model/provider/network/frontend/mobile/remote/plugin capability, dependency changes, or production Control Center authority.
- v0.17.4 Web Control Center local browser smoke polish keeps the existing shell read-only/preview-only. It may improve route headings, accessible loading/empty states, mock fallback reviewability, action preview risk metadata display, frontend tests, and safe local browser smoke reporting docs only. Do not add backend API paths, dependencies, runtime/model/provider/network execution, remote dispatch, mobile/native/sensor code, plugin enablement, Chrome authenticated profile control, Computer Use automation, iOS/macOS build workflow, generated artifacts, or production Control Center authority.
- v0.17.5 roadmap projection and milestone charter freeze is docs/canonical/verifier/gate-only. It resolves M14 as Web Control Center Local Backend Connection Stabilization, moves Approval Queue + Receipt/Event Viewer UI to M15, and records M14-M20 as planned/provisional but canonical until superseded by a reviewed roadmap patch. Do not use this patch to implement M14, add frontend features, backend API routes, runtime/model/provider/network execution, remote dispatch, mobile/native/sensor code, plugin enablement, dependencies, architecture changes, Chrome authenticated profile control, Computer Use automation, iOS/macOS build workflow, generated artifacts, or production Control Center authority.
- v0.18.0 Web Control Center local backend connection stabilization implements M14 in the existing shell only. API base URL policy must allow only relative paths, localhost, 127.0.0.1, and loopback IPv6; external absolute API URLs and secret-like API base strings must be blocked or rejected. Mock fallback must be visibly mock and non-authoritative, and live/degraded/mock/offline-safe states must be clear. Do not add M15 Approval Queue + Receipt/Event Viewer UI, backend API routes, OpenAPI path count changes, runtime/model/provider calls, remote dispatch, mobile sensor access, plugin enablement, auth, credentials, cookies, Authorization headers, API keys, analytics/SaaS SDKs, external API hosts, dependencies, native build workflows, or production Control Center authority.
- v0.18.1 Web Control Center local backend connection safety hardens M14 in the existing shell only. API base URL policy must keep only relative paths, localhost, 127.0.0.1, and loopback IPv6 allowed; public IPs, private LAN IPs, non-loopback hostnames, URL credentials, external absolute API URLs, and broad secret-like query parameters must be blocked or rejected. Mock fallback must be visibly mock and non-authoritative, and unknown/checking/live/degraded/mock/offline-safe states must be clear. Do not add M15 Approval Queue + Receipt/Event Viewer UI, backend API routes, OpenAPI path count changes, runtime/model/provider calls, remote dispatch, mobile sensor access, plugin enablement, auth, credentials, cookies, Authorization headers, API keys, analytics/SaaS SDKs, external API hosts, dependencies, native build workflows, or production Control Center authority.
- v0.18.2 Open Design System and UI Design Governance is docs/governance only. Design docs, reviewed components, and future repo-owned tokens are the design source of truth. Figma, Stitch, Framer, design-to-code, screenshot-to-code, AI UI generators, design SaaS, external design sync, and automatic design-to-code commits are not enabled and are not authority. Do not add M15 Approval Queue + Receipt/Event Viewer UI, frontend behavior, backend API routes, OpenAPI path count changes, runtime/model/provider calls, remote dispatch, mobile sensor access, plugin enablement, auth, credentials, cookies, Authorization headers, API keys, analytics/SaaS SDKs, external API hosts, dependencies, native build workflows, Chrome authenticated profile control, Computer Use automation, design tool integration, or production Control Center authority.
- v0.18.3 OpenWebUI and CCC Client Strategy clarification is docs/roadmap/governance only. OpenWebUI is the preferred conversational web shell, not the agent brain. CCC means Control Center Clients: CCC Web, CCC iOS, CCC Android, and CCC macOS. CCC Web is the current TypeScript web Control Center; CCC iOS, CCC Android, and CCC macOS are future native clients only. Open Design governs custom CCC surfaces and does not replace OpenWebUI. Do not add M15 Approval Queue + Receipt/Event Viewer UI, OpenWebUI integration, OpenWebUI deployment config, backend API routes, frontend features, OpenAPI path count changes, native CCC implementation, Android app, iOS app, macOS app, Kotlin, Java, Swift, AppKit, SwiftUI, React Native, Expo, Flutter, Gradle, Android Studio, native package, mobile sensor access, OS permission integration, background services, notification runtime/channel, signing, keystore, provisioning, keychain, entitlements, App Store/Play Store workflow, native build workflow, dependencies, plugin enablement, runtime/model/provider/network/remote execution, Chrome authenticated profile control, Computer Use automation, or production authority.
- v0.18.4 post-M20 roadmap projection is docs/roadmap/canonical planning only. M14-M20 remain frozen and unchanged. M21-M40 are planned/provisional charters only and do not implement OpenWebUI bridge code, local model runtime activation, real local LLM calls, memory provider storage, truth/evidence router expansion, sandbox execution, tool execution, MCP runtime support, Agent Skills runtime support, AGENTS.md runtime loading, CCC native clients, device pairing, Device Capability Broker implementation, mobile capture, sensors, browser automation, observability exports, or eval harnesses. Do not add M15, backend API routes, frontend features, runtime execution, local model execution, model/provider calls, network calls, remote execution, remote worker dispatch, mobile app code, Android/iOS/macOS app code, mobile sensor APIs, OS permission integration, native build workflows, Computer Use, browser automation, plugin enablement, dependencies, or architecture behavior changes.
- v0.19.0 M15 Approval Queue + Receipt/Event Viewer UI adds read-only/preview-only CCC Web inspection panels for approval request summaries, receipt summaries, and event summaries, with selected detail panels, visibly mock non-authoritative fallback data, frontend tests, static safety verification, documentation, and Foundation Gate coverage. M15 adds no backend API routes and keeps OpenAPI path count unchanged. Do not use M15 to add approval execution, approval grant/reject mutation, send/write/run/deploy/enable controls, backend authority bypass, arbitrary approval_ref authority, raw secret/prompt/file/memory/credential/provider payload display, runtime/model/provider/network execution, remote dispatch, mobile/native/sensor code, plugin enablement, auth, credentials, cookies, analytics/SaaS SDKs, external API hosts, dependencies, native build workflows, Chrome authenticated profile control, Computer Use automation, scanner runtime, Skill Factory, self-improvement, production persistence, or external actions.
- v0.19.1 M15 Approval/Receipt UI safety hardening strengthens CCC Web authority-boundary and redaction checks only. Approval refs are identifiers only and never authority; Python Agent Core remains the only approval authority; receipt/event detail views must remain redacted summary metadata only. Do not use v0.19.1 to start M16 Event Timeline + Run/Receipt Trace Viewer, add approval execution, approve/deny mutation, send/write/run/deploy/enable controls, backend API routes, OpenAPI path count changes, runtime/model/provider/network execution, remote execution, mobile sensor access, plugin enablement, dependencies, native build workflows, raw M15 review fields, credential-like review fields, authority bypass, or production Control Center authority.
- v0.20.0 M16 Event Timeline + Run/Receipt Trace Viewer adds a read-only `/events/timeline` CCC Web surface only. It may show redacted timeline summaries, safe event refs, run refs, correlation refs, receipt refs, relation refs, and Foundation Gate evidence summaries. Do not use v0.20.0 to add approval execution, tool execution, model/provider calls, remote execution, mobile sensor access, plugin enablement, backend API routes, OpenAPI path count changes, raw secret/prompt/file/memory/credential/provider payload display, raw event payload dumps, production telemetry export, external observability integration, OpenTelemetry export, cloud traces, dependencies, native build workflows, or production Control Center authority.
- v0.20.1 M16 trace/redaction safety hardening keeps `/events/timeline` read-only and frontend-only. It may add tests, docs, static verifier checks, and Foundation Gate checks for alternate trace selection, redacted summary-only display, OpenAPI path count, and no backend timeline/raw/export routes. Review builds should prefer temporary Vite output paths such as `/tmp/uaa-control-center-review-dist`, and generated frontend build/log artifacts must remain ignored and untracked. Do not use v0.20.1 to start M17 Evidence/File/Memory Viewer, add backend API routes, runtime execution, model/provider calls, remote execution, mobile sensor access, plugin enablement, telemetry export, external observability integration, raw secret/prompt/file/memory/credential/provider payload display, dependencies, native build workflows, or production Control Center authority.
- v0.21.0 M17 Evidence/File/Memory Viewer adds read-only `/evidence`, `/files`, and `/memory` CCC Web surfaces only. It may show redacted evidence refs, safe file refs, memory refs, relation refs, status, confidence, staleness, conflict indicators, data classification, and safe summaries. Memory is recall, not authority; canonical files and governed source systems outrank memory. Do not use v0.21.0 to add backend API routes, OpenAPI path count changes, file mutation, memory mutation, filesystem browsing, raw secret/prompt/file/memory/evidence/credential/provider payload display, embeddings, vector DB, memory provider implementation, runtime/model/provider calls, remote execution, mobile sensor access, plugin enablement, dependencies, native build workflows, or production Control Center authority.
- v0.21.1 M17 Evidence/File/Memory Viewer safety hardening keeps `/evidence`, `/files`, and `/memory` frontend-only, read-only, visibly mock, non-authoritative, and redacted summary-only. It may add alternate safe mock refs, selected-card reviewability, tests, static verifier checks, docs, browser-smoke reviewability, and Foundation Gate criteria. Do not use v0.21.1 to start M18 Local Runtime Status + Manual Smoke Control Surface, add backend API routes, change OpenAPI path count from 74, add file mutation, memory mutation, filesystem browsing, raw secret/prompt/file/memory/evidence/credential/provider payload display, embeddings, vector DB, memory provider implementation, runtime/model/provider calls, remote execution, mobile sensor access, plugin enablement, dependencies, auth, credentials, cookies, analytics, SaaS SDKs, native build workflows, or production Control Center authority.
- v0.21.2 Developer Environment Command Normalization is dev tooling/docs only. It may add repo-local Makefile targets, `scripts/verify_dev_environment.py`, tests, and docs so verification uses `.venv/bin/python` rather than a bare `python` binary. Do not use v0.21.2 to start M18, add runtime features, frontend features, backend API routes, dependencies, global tool installs, application behavior changes, runtime/model/provider calls, network calls, mobile/native/browser/computer-use functionality, plugin enablement, or production capability.
- v0.22.0 M18 Local Runtime Status + Manual Smoke Control Surface adds read-only `/runtime/local` and validation-only `/runtime/manual-smoke` CCC Web surfaces only. It may show runtime readiness metadata, capability matrix summaries, safe manual smoke report validation summaries, fixed prompt hash values, redaction status, and visibly mock non-authoritative fallback data. Do not use v0.22.0 to add backend API routes, change OpenAPI path count from 74, execute runtimes, execute manual smoke, start/stop/connect/launch local runtimes, call models/providers, integrate local runtime providers, dispatch remote work, access mobile sensors, enable plugins, integrate OpenWebUI, add scanner runtime, Skill Factory, self-improvement, production persistence, dependencies, raw smoke report display, raw prompts, raw response bodies, credentials, provider payloads, native build workflows, or production Control Center authority.
- v0.22.1 Roadmap Status Label Cleanup After M18 is docs/version/verifier/Foundation Gate documentation hygiene only. It may mark v0.22.0 / M18 implemented, keep M19/M20 and M21-M40 planned/provisional, update active release/import/master/Foundation Gate docs, and add documentation-integrity coverage for those labels. Do not use v0.22.1 to start M19, add backend API routes, frontend behavior, OpenAPI path count changes, runtime execution, manual smoke execution, model/provider calls, remote execution, mobile sensor access, plugin enablement, OpenWebUI integration, dependencies, architecture behavior changes, native build workflows, or production Control Center authority.
- v0.23.0 M19 Mobile Companion Contract/API Planning adds Python contract models, validation helpers, docs, tests, verifier coverage, and Foundation Gate criteria for future CCC iOS/Android/mobile companion planning only. Do not use M19 to add M20 Device Capability Broker implementation, backend API routes, frontend behavior, Android app, iOS app, macOS app, native build workflows, OS permission integration, mobile sensor access, mobile approval execution, runtime execution, manual smoke execution, model/provider calls, remote execution, plugin enablement, OpenWebUI integration, dependencies, architecture behavior changes, or production Control Center authority.
- v0.23.1 M19 Roadmap Status + Mobile Contract Safety Hardening is cleanup/version/verifier/Foundation Gate/test/docs work only. It may mark v0.23.0 / M19 implemented/released, keep M20 and M21-M40 planned/provisional, add contacts/calendar denial coverage, reject secret-like metadata refs, reject external sends, reject OS permission integration, and reject background services. Do not use v0.23.1 to add Device Capability Broker implementation, backend API routes, mobile app code, Android app code, iOS app code, macOS app code, native build workflows, mobile sensor APIs, OS permission integration, background services, notification runtime, runtime execution, model/provider calls, remote execution, mobile approval execution, plugin enablement, dependencies, architecture behavior changes, or production authority.
- v0.24.0 M20 Device Capability Broker Contract adds contract-only device capability models, manifest builders, validation helpers, docs, tests, static verifier coverage, and Foundation Gate criteria for future CCC iOS, CCC Android, CCC macOS, and mobile web/PWA device capability planning only. Do not use M20 to add Device Capability Broker runtime implementation, backend API routes, frontend behavior, Android app, iOS app, macOS app, native build workflows, sensor APIs, OS permission integration, background services, notification runtime, device pairing runtime, mobile storage runtime, runtime execution, model/provider calls, remote execution, mobile approval execution, plugin enablement, OpenWebUI integration, dependencies, architecture behavior changes, or production authority.
- v0.24.1 M20 Device Capability Broker Contract Safety Hardening strengthens validators, tests, docs, static verification, and Foundation Gate coverage only. It rejects enabled and implemented capability flags, permission runtime claims, notification push runtime claims, background service runtime claims, validation-decision allow claims, non-redacted receipt plans, non-contract revocation plans, raw payload-like metadata, geolocation coordinates, private local paths, and secret-like metadata. Do not use v0.24.1 to start M21, add Device Capability Broker runtime implementation, backend API routes, frontend behavior, Android app, iOS app, macOS app, native build workflows, sensor APIs, OS permission integration, background services, notification runtime, device pairing runtime, mobile storage runtime, runtime execution, model/provider calls, remote execution, mobile approval execution, plugin enablement, OpenWebUI integration, dependencies, architecture behavior changes, or production authority.
- v0.25.0 M21 OpenWebUI Bridge + Chat Shell Integration Contract adds contract/planning/validation-only OpenWebUI bridge models, validation helpers, docs, tests, static verifier coverage, and Foundation Gate criteria. OpenWebUI is the preferred conversational web shell, not the agent brain; Python Agent Core remains authority; OpenWebUI refs are identifiers only and never authority. Do not use v0.25.0 to add OpenWebUI integration, deployment config, Docker config, OpenWebUI plugins/functions/pipelines/tools/admin/auth/cookies/API keys/admin tokens, browser profile access, live OpenWebUI connection, backend API routes, frontend features, OpenWebUI runtime execution, user-content local LLM calls, model/provider calls, tool execution, memory writes, file access, remote execution, browser automation, Computer Use, mobile sensor access, plugin enablement, dependencies, or production authority. v0.26.0 implements M22 contract-only; v0.27.0 implements M23 manual fixed-prompt local call only.
- v0.25.1 M21 OpenWebUI Bridge Contract Safety Hardening clarifies blocked raw-content modes, allows safe negated authority-boundary text, rejects positive OpenWebUI authority claims, scans the OpenWebUI bridge package for forbidden runtime/config fragments, and recursively rejects forbidden OpenWebUI config/path names outside docs. Do not use v0.25.1 to add OpenWebUI integration, deployment config, Docker config, OpenWebUI plugins/functions/pipelines/tools/admin/auth/cookies/API keys/admin tokens, browser profile access, live OpenWebUI connection, backend API routes, frontend features, OpenWebUI runtime execution, user-content local LLM calls, model/provider calls, tool execution, memory writes, file access, remote execution, browser automation, Computer Use, mobile sensor access, plugin enablement, dependencies, or production authority. v0.26.0 implements M22 contract-only; v0.27.0 implements M23 manual fixed-prompt local call only.
- v0.26.0 M22 Local Model Runtime Activation Contract adds contract/planning/validation-only local runtime activation models, planned-disabled provider profiles, loopback/relative metadata endpoint policy, health probe plan validation, tests, docs, static verifier coverage, and Foundation Gate criteria. No model was called, no runtime was activated, and no endpoint was contacted. Do not use v0.26.0 to add runtime execution, endpoint probes, provider SDK imports, runtime package imports, model loading, user prompt processing, backend API routes, OpenWebUI runtime behavior, tool execution, memory writes, file writes, dependencies, or production authority. v0.27.0 implements M23 manual fixed-prompt local call only.
- v0.26.1 M22 Safety Hardening tightens M22 verifier fragments, validates metadata keys as well as values in local runtime activation policy/request/decision contracts, removes brittle local route-count unit-test ownership, and cleans duplicate M22 docs wording. Do not use v0.26.1 to add runtime execution, endpoint probes, provider SDK imports, runtime package imports, model loading, user prompt processing, backend API routes, OpenWebUI runtime behavior, tool execution, memory writes, file writes, dependencies, or production authority.
- v0.27.0 M23 First Real Local LLM Call adds a manual/CLI-only, loopback-only, fixed-prompt-only local model call path that is dry-run by default, requires `--execute-local-call`, requires validated local approval for execution, stores no raw response, and marks model output non-authoritative. Tests and Foundation Gate use fake transport only. Do not use v0.27.0 to add backend API routes, OpenAPI path changes, runtime activation, endpoint probes, arbitrary prompts, user-content model calls, stdin/file/clipboard/memory/OpenWebUI transcript prompts, provider SDK imports, runtime package imports, model loading, Control Center execution controls, OpenWebUI runtime bridges, tool execution, memory writes, file writes, remote execution, mobile sensor access, plugin enablement, dependencies, or production authority.
- v0.27.1 M23 Local LLM Call Safety Hardening tightens endpoint-label safety, secret-like endpoint/query rejection, forged approval resistance, response redaction/caps, CLI guardrails, policy docs, static verifier coverage, Foundation Gate criteria, and Foundation Gate report atomic write/replace safety. The report-write fix is tooling/test hardening only and was not a v0.27.0 release blocker. Do not use v0.27.1 to add backend API routes, OpenAPI path changes, runtime activation, endpoint probes, arbitrary prompts, user-content model calls, stdin/file/clipboard/memory/OpenWebUI transcript prompts, provider SDK imports, runtime package imports, model loading, Control Center execution controls, OpenWebUI runtime bridges, tool execution, memory writes, file writes, remote execution, mobile sensor access, plugin enablement, dependencies, runtime behavior expansion, M24 work, or production authority.
- v0.28.0 M24 Memory Provider Abstraction + Local Memory Store adds governed local memory provider contracts, local in-memory/dev storage, explicit-path stdlib SQLite local storage, reviewed-write validation, source priority, provenance/source/evidence/event/receipt refs, trust/confidence metadata, dedup/decay/archive planning metadata, recall-planning metadata, retention/delete/export contracts, tests, docs, static verifier coverage, and Foundation Gate coverage. Memory is recall, not authority. Memory is not ground truth. Canonical files, evidence manifests, receipts, Event Ledger records, and user-reviewed sources outrank memory. Do not use v0.28.0 to add automatic memory writes, model-output writes, local LLM output writes, OpenWebUI chat memory writes, Control Center memory mutation, mobile capture writes, tool output writes, cloud memory providers, vector DB, embeddings, semantic search, RAG ingestion, raw session history, context injection, broad filesystem scanning, backend mutation routes, dependencies, production persistence, runtime/model/provider execution, remote execution, mobile sensor access, plugin enablement, M25 claim verification, or production authority.
- v0.28.2 M24 duplicate roadmap row cleanup is docs/version-only. It removes the duplicate/conflicting v0.28.1 planned/provisional roadmap row and keeps the implemented/released v0.28.1 row. Do not use v0.28.2 to add code behavior, tests, dependencies, backend routes, OpenAPI path changes, runtime/model/provider calls, memory authority expansion, M25 work, or production authority.
- v0.29.0 M25 Truth Source Router + Evidence Claim Checker adds deterministic local truth source contracts, source priority ordering, claim/evidence/verification models, evidence chain validation, conflict/staleness/revocation handling, tests, docs, static verifier coverage, and Foundation Gate coverage over explicitly provided refs only. Memory is recall, not authority. Model/runtime/OpenWebUI/Control Center output cannot verify truth. Arbitrary refs cannot self-authorize claims. Verified status requires primary/source-backed evidence. Do not use v0.29.0 to add web search, external verification, source fetching, retrieval/RAG/vector/embedding behavior, model/provider calls, local LLM calls, source crawling, memory writes, evidence mutation, backend API routes, OpenAPI path changes, Control Center mutation, dependencies, M26 grounded recall/context-pack work, or production authority.
- v0.29.1 M25 Truth Source Router + Evidence Claim Checker safety hardening rejects unknown and arbitrary truth source refs for all verification-success statuses, denies explicit TruthSourceKind.unknown evidence, keeps self-verification denied, and strengthens Foundation Gate/static verifier coverage for those cases. Evidence-supported and verified status require recognized structured source/evidence refs. Do not use v0.29.1 to add web search, external verification, source fetching, retrieval/RAG/vector/embedding behavior, model/provider calls, local LLM calls, source crawling, memory writes, evidence mutation, backend truth verification routes, OpenAPI path changes, Control Center mutation, dependencies, M26 grounded recall/context-pack work, or production authority.
- v0.29.2 M25 local-dev API authority and raw preview safety hardening removes test-prefixed approval-ref fallback authority from Tool Broker/kernel mutation paths, keeps public `/kernel/tasks/run` local-dev mutation requests dry-run-only, returns metadata-only file read previews by default, sanitizes API exception safe messages, and makes direct truth memory/model authority helpers fail closed. Do not use v0.29.2 to add web search, external verification, source fetching, retrieval/RAG/vector/embedding behavior, model/provider calls, local LLM calls, source crawling, memory writes, evidence mutation, backend truth verification routes, backend route expansion, OpenAPI path changes, Control Center mutation, dependencies, M26 grounded recall/context-pack work, or production authority.
- v0.29.3 documentation archive structure and active/historical classification is docs/index/verifier/version/file-organization only. It moves historical root release packets under `docs/archive/releases/`, moves retired root planning packets under `docs/archive/retired_plans/`, adds docs/archive entrypoints, keeps active release notes and implementation plans in their existing docs folders, and marks the legacy v0.17.5 roadmap sequence as historical while preserving compatibility. Do not use v0.29.3 to start M26, add Grounded Recall Router, Context Pack Builder, runtime behavior, backend routes, frontend features, dependencies, model/provider calls, retrieval/RAG/vector/embedding functionality, memory writes, tool execution, security architecture changes, or production authority.
- v0.29.4 documentation archive reference repair and self-maintaining docs policy is docs/index/verifier/version/file-organization only. It archives active-looking historical version verifiers, removes stale Ruff excludes for retired verifier paths, adds `docs/maintenance/DOCUMENTATION_ORGANIZATION_POLICY.md`, updates archive/index/release docs, and hardens documentation-integrity checks so active validation cannot depend on moved root historical release artifacts. Do not use v0.29.4 to start M26, add Grounded Recall Router, Context Pack Builder, runtime behavior, backend routes, frontend features, dependencies, model/provider calls, retrieval/RAG/vector/embedding functionality, memory writes, tool execution, security architecture changes, or production authority.
- v0.30.0 M26 Grounded Recall Router + Evidence-Linked Context Pack Builder adds deterministic local recall/context-pack contracts over provided safe candidates only. It ranks source-backed refs above memory, excludes unknown/arbitrary/stale/conflicted/revoked/deleted/model/runtime/OpenWebUI/raw/secret candidates by default, builds safe summary-only context packs, and adds tests, docs, static verifier coverage, and Foundation Gate criteria. Do not use v0.30.0 to add backend routes, frontend features, vector search, embeddings, semantic search, RAG ingestion, web search, external retrieval, source crawling, arbitrary file reads, model/provider calls, local LLM calls, memory writes, evidence mutation, Event Ledger mutation, context injection runtime, OpenWebUI runtime bridge, dependencies, tool execution, M27 MCP/Agent Skills/AGENTS.md runtime, plugin enablement, or production authority.
- v0.30.1 M26 recall source identity hardening enforces source_ref/source_kind consistency, denies mismatched memory/model/runtime/OpenWebUI refs, and prevents caller-declared source_kind from upgrading source priority. Do not use v0.30.1 to add M27, Tool Broker v2, agent planning, tool execution, vector search, embeddings, semantic search, RAG, web/external retrieval, model/provider calls, memory writes, context injection runtime, backend recall/search/injection routes, dependencies, or production authority.
- v0.31.0 M27 Tool Broker v2 + Safe Tool Intent Contracts adds validation-only and preview-only typed tool intent, catalog, target, input-boundary, decision, manifest, and non-executing receipt-plan contracts. Approval refs are identifiers only and not authority; context packs are not authority; decisions cannot execute tools, perform side effects, write memory, mutate the Event Ledger, call models/providers, make network calls, control browsers, enable plugins, inject context, add backend execution routes, add dependencies, or create production authority.
- v0.31.1 GitHub README Polish Baseline Normalization is docs/version/release-metadata only. Do not use it to add Approval Authority v2 implementation, action execution, backend routes, dependencies, or production authority.
- v0.32.0 M28 Approval Authority v2 + Action Policy Expansion adds contract/policy/decision-only approval authority and action policy contracts. Approval decisions are not action execution; approval_ref, approval_test_ refs, and consent_ref alone are not authority; wildcard, expired, revoked, replayed, and actor/action/resource/scope mismatched grants are denied. Do not use v0.32.0 to add action execution, tool execution, shell/subprocess execution, file mutation, memory writes, network calls, model/provider calls, browser/mobile/remote/plugin execution, backend action/tool execution routes, dependencies, M29 work, or production authority. M29-M40 remain planned/provisional.
- v0.32.1 M28 Evaluator Revalidation Hardening revalidates ActionIntent, ApprovalGrant, and ActionPolicy objects before policy-only allow decisions so model_copy mutations cannot smuggle raw prompt/model/file/transcript flags, secret-like summaries or metadata, metadata refs, approval_test_ grant refs, expired/revoked/replayed grants, wildcard scope, or mismatched bindings into allowed decisions. Do not use v0.32.1 to add action execution, tool execution, shell/subprocess execution, file mutation, memory writes, network calls, model/provider calls, browser/mobile/remote/plugin execution, backend action/tool execution routes, dependencies, M29 work, or production authority. M29-M40 remain planned/provisional.
- v0.33.0 M29 Agent Task Planning Engine adds deterministic local task planning contracts for task goals, task steps, task plans, dependency graph validation, input boundaries, risk/authority checks, review-only decision envelopes, and non-authoritative receipt plans. Plans may be valid for review only. Do not use v0.33.0 to add task execution, auto-run, scheduler runtime, tool execution, action execution, shell/subprocess execution, file mutation, memory writes, network calls, model/provider calls, browser/mobile/remote/plugin execution, backend task/plan execution routes, Control Center execute controls, dependencies, context injection, M30 work, or production authority. M30-M40 remain planned/provisional.
- v0.33.1 M29 Task Plan Dependency, Risk, and No-Execution Safety Hardening strengthens dependency graph validation, duplicate/missing step denial, self/indirect cycle detection, derived risk checks, hidden side-effect denial, authority-boundary checks, evaluator revalidation, static verification, and Foundation Gate coverage. Do not use v0.33.1 to add task execution, action execution, tool execution, scheduler/background worker, shell/subprocess execution, file mutation, memory writes, network calls, model/provider calls, browser/mobile/remote/plugin execution, backend task/plan execution routes, Control Center execute controls, dependencies, M30 work, or production authority. M30-M40 remain planned/provisional.
- v0.34.0 M30 Multi-Step Execution Framework adds deterministic local side-effect-safe state-machine-only contracts for execution runs, execution steps, transition requests, transition decisions, dependency-aware no-effect progression, replay protection, evaluator revalidation, and non-authoritative receipt plans. Do not use v0.34.0 to add real task execution, action execution, tool execution, shell/subprocess execution, file mutation, memory writes, Event Ledger mutation, network calls, model/provider calls, browser/mobile/remote/plugin actions, scheduler/background worker/daemon, autonomous loops, context injection runtime, backend execution routes, Control Center execute controls, dependencies, M31 work, or production authority. M31-M40 remain planned/provisional.
- v0.34.1 M30 Execution State Machine, Replay, and No-Side-Effect Safety Hardening strengthens ready-only no-effect completion, invalid transition denial, incomplete finalize denial, replay-key and transition-id denial, hidden side-effect denial, evaluator revalidation, static verification, and Foundation Gate coverage. Do not use v0.34.1 to add real task execution, action execution, tool execution, shell/subprocess execution, file mutation, memory writes, Event Ledger mutation, network calls, model/provider calls, browser/mobile/remote/plugin actions, scheduler/background worker/daemon, autonomous loops, context injection runtime, backend execution routes, Control Center execute controls, dependencies, M31 work, or production authority. M31-M40 remain planned/provisional.
- v0.35.0 M31 Real Tool Runtime Adapter, Single Safe No-Op Tool adds a governed local tool runtime adapter for `tool:no_op.v1` only. The no-op adapter may return a deterministic completion result and redacted receipt plan with `side_effects_performed=[]`; it must not dispatch arbitrary tools, dynamically load callables/modules, run shell/subprocess code, mutate files, write memory or Event Ledger records, call networks/models/providers, use browser/mobile/remote/plugin actions, add backend execute routes, add Control Center execute controls, add dependencies, start M32, or claim production authority. Approval refs, `approval_test_*`, task plans, tool intents, context packs, memory, model/runtime/OpenWebUI output, and Control Center refs are identifiers or context only and cannot authorize runtime tool invocation. M32 was later implemented by v0.36.0, M33 was later implemented by v0.37.0, and M34 was later implemented by v0.38.0 as planning/docs/verifier only; M35-M60 remain planned/provisional.
- v0.35.1 M31 No-Op Tool Runtime Adapter Safety hardens no-op runtime allowlist validation, tool_ref/tool_name consistency, dynamic dispatch denial, hidden side-effect denial, authority-boundary checks, evaluator revalidation, replay protection, static verification, documentation, and Foundation Gate coverage. Do not use v0.35.1 to add arbitrary tool execution, side-effecting tools, shell/subprocess execution, file mutation, memory writes, Event Ledger mutation, network calls, model/provider calls, browser/mobile/remote/plugin tools, backend execute routes, Control Center execute controls, dependencies, M32 work, or production authority.
- v0.36.0 M32 Safe Tool Runtime Expansion adds exactly one safe local filesystem metadata tool, `tool:filesystem_metadata.v1`, through the governed runtime adapter. It may inspect metadata under server-owned safe roots and return safe path refs, existence, kind, size, extension, and modified-time metadata only. It must not read raw file content, return text previews, hash content, list directories, recurse, follow symlinks, accept caller-selected arbitrary roots, use absolute/traversal/hidden/secret-like/glob paths, mutate files, write memory or Event Ledger records, run shell/subprocess code, call networks/models/providers, use browser/mobile/remote/plugin actions, add backend file/tool execution routes, add Control Center execute controls, add dependencies, start M33, or claim production authority. Approval refs, `approval_test_*`, task plans, execution state, tool intents, context packs, memory, model/runtime/OpenWebUI output, and Control Center refs cannot authorize filesystem metadata access or any execution. M33 was later implemented by v0.37.0 and M34 was later implemented by v0.38.0 as planning/docs/verifier only; M35-M60 remain planned/provisional.
- v0.36.1 M32 Filesystem Metadata Path Safety hardening strengthens path normalization, encoded traversal denial, home/Windows/double-separator path denial, hidden/private-key-like path denial, caller-selected root denial, metadata alias flag denial, model_copy evaluator revalidation, static verification, documentation, and Foundation Gate coverage. Do not use v0.36.1 to add raw file reads, text previews, content hashes, directory listing, recursive traversal, symlink following, arbitrary tool execution, shell/subprocess execution, file mutation, memory writes, Event Ledger mutation, network calls, model/provider calls, browser/mobile/remote/plugin tools, backend execute/raw-file routes, Control Center execute/raw-preview controls, dependencies, M33 work, or production authority.
- v0.37.0 M33 First Safe Local File Read Proposal, Redacted Preview Only adds exactly one governed redacted-preview proposal tool, `tool:filesystem.redacted_preview.v1`, through the tool runtime adapter. It may read only bounded preview bytes inside the redaction-before-return boundary under server-owned safe roots and relative path policy. It must not return or store raw file content, return full-file output, compute content hashes, list directories, recurse, follow symlinks, accept caller-selected arbitrary roots, read hidden or secret-like paths, mutate files, write memory or Event Ledger records, run shell/subprocess code, call networks/models/providers, perform context injection, use browser/mobile/remote/plugin actions, add backend raw-file/tool execution routes, add Control Center raw-preview/execute controls, add dependencies, start M34, or claim production authority. Approval refs, `approval_test_*`, task plans, execution state, tool intents, context packs, memory, model/runtime/OpenWebUI output, and Control Center refs cannot authorize arbitrary filesystem preview access or any execution.
- v0.37.1 M33 Redacted File Preview Safety hardens M33 only. It rejects unredacted secret-like preview text at the `RedactedFilePreviewOutput` boundary, denies symlink safe roots before preview, strengthens static verification and Foundation Gate probes, and preserves evaluator revalidation. Do not use v0.37.1 to add raw file output, full-file reads, content hashing, directory traversal/listing, symlink following, caller-selected arbitrary roots, file mutation, arbitrary filesystem tools, shell/subprocess execution, memory writes, network calls, model/provider calls, backend raw-file/execute routes, Control Center raw-preview/execute controls, context injection, dependencies, M34 work, or production authority.
- v0.37.2 Local Developer Launcher + Desktop Shortcut is developer tooling only. It may add repo-local launcher scripts, ignored `.uaa/dev/` PID/log state, docs, tests, static verification, and a macOS `.command` launcher generator for starting the existing localhost FastAPI backend and CCC Web dev server. Do not use v0.37.2 to add production installer behavior, launch daemons, backend routes, Control Center execute controls, task/action/tool execution, arbitrary shell execution beyond starting existing local dev servers, file mutation beyond launcher-local PID/log files, memory writes, network/provider/model calls, dependencies, M34 work, or production authority.
- v0.38.0 M34 Broader File Capability Review is planning, architecture review, documentation, verifier, and Foundation Gate work only. It may add active file capability review docs, boundary matrix, risk register, decision record, M35 readiness guidance, M34-to-M35 boundary docs, release metadata, documentation-integrity checks, static verifier checks, and Foundation Gate criteria. Do not use v0.38.0 to add Safe File Review Workflow Contracts, CCC File Review Surface, Review Approval Capture, context proposals, context handoff, new runtime file capability, raw file reads, redacted preview behavior changes, file writes/deletes/mutation, directory listing, recursive traversal, symlink following, arbitrary caller-selected roots, file browser/picker/upload/export/download/copy-raw controls, context injection, memory writes, tool/action execution, shell/subprocess, network calls, provider/model calls, OpenWebUI runtime integration, mobile/TestFlight work, native app work, sensors, browser automation, plugins, dependencies, backend routes, frontend runtime features, or production authority. M35-M60 remain planned/provisional.
- v0.38.1 M34 File Capability Review Boundary Clarity hardens docs, static verification, and Foundation Gate checks only. It rejects active docs that still list v0.38.0 / M34 as planned/provisional or say M34 remains planned/provisional after v0.38.0, and it rejects any active claim that M34 implements Safe File Review Workflow Contracts, File Review Control Center Surface, approval capture/persistence, context proposal, context injection, raw file access, memory writes, export, execution, or runtime file authority. Do not use v0.38.1 to start M35, add review workflow contracts, add file review UI, add approval persistence, add context proposal/injection, add raw/full/unredacted file output, add memory writes/export/execution/file mutation, add backend raw-file/review/context/execute routes, add Control Center raw-preview/file-review/execute controls, add dependencies, or claim production authority. M35-M60 remain planned/provisional.
- v0.38.2 M34 Current-Baseline Label Repair is docs/verifier/Foundation Gate/release metadata only. It repairs stale v0.38.0/v0.38.1 current-baseline labels and keeps M35-M60 planned/provisional. Do not use v0.38.2 to add file review workflow contracts, UI, approval persistence, context proposal/injection, raw file access, memory writes, export, execution, backend routes, dependencies, or production authority.
- v0.39.0 M35 Safe File Review Workflow Contracts adds review-only, contract-only file review workflow models over already-redacted preview results. It may package redacted previews into review packets, verify redaction metadata, evaluate exact user approval binding, produce review-only decision envelopes, and create no-raw receipt plans. Do not use v0.39.0 to add Control Center file review UI, approval capture, approval persistence, context proposal, context injection, memory writes, export, execution, raw file access, full-file reads, unredacted previews, file mutation, backend routes, dependencies, M36 work, M37 work, M38 work, or production authority. M36-M60 remain planned/provisional.
- v0.39.1 M35 File Review Exact File/Path Binding hardening requires review approvals to bind to the exact actor, review packet, preview result, redaction summary, file_ref, and safe_path_ref. It denies model_copy-mutated packet file/path refs at evaluator boundaries and adds tests, static verification, documentation-integrity checks, and Foundation Gate coverage. Do not use v0.39.1 to add Control Center file review UI, approval capture, approval persistence, context proposal, context injection, memory writes, export, execution, raw file access, full-file reads, unredacted previews, file mutation, backend routes, dependencies, M36 work, M37 work, M38 work, or production authority. M36-M60 remain planned/provisional.
- v0.40.0 M36 CCC File Review Surface, Review-Only adds a frontend-only Control Center `/files/review` surface for redacted review packets. It may display redacted previews, redaction summaries, exact binding refs, review-only decision status, approval gate contract status, receipt plan metadata, and visibly mock non-authoritative fallback data. Do not use v0.40.0 to add approval capture, approval persistence, backend review routes, raw file reads, raw file display/storage, full-file reads, file picker/browser/upload/root selector, export/download/copy-raw controls, context proposal, context injection, memory writes, execution/tool controls, file mutation, dependencies, M37 work, M38 work, or production authority. M37-M60 remain planned/provisional.
- v0.41.0 M37 Review Approval Capture, Review-Only Persistence adds review-only approval and denial capture for exact redacted review packets. It may use exactly one backend route, `POST /files/review/approvals/capture`, and safe-ref-only local review approval persistence. It does not grant raw file access, context proposal, context injection, memory writes, export, execution, tool authority, arbitrary filesystem access, dependencies, M38 work, or production authority.
- v0.42.0 M38 Safe Context Proposal From Approved Review adds core context proposal contracts, validation, decisions, receipt plans, tests, docs, static verification, and Foundation Gate coverage. It may create safe, non-authoritative, proposal-only context bundles from exact-scope approved redacted review records. It does not add context injection, OpenWebUI handoff, model/provider calls, memory writes, raw file reads/output/storage, full-file reads, unredacted previews, arbitrary filesystem reads, export/download behavior, tool/action/task execution, shell/subprocess/network calls, backend context proposal/injection/model/OpenWebUI routes, Control Center context proposal surface, dependencies, M39 work, M40 work, or production authority. M39-M60 remain planned/provisional.
- API validation errors must be sanitized and must never echo raw invalid input values or secret-like field values.

## Workspace

- Keep milestone changes small and release-gated.
- Prefer typed contracts, deterministic tests, and metadata-only verification before implementation work.
- Prefer `make doctor`, `make test`, `make verify`, and `make frontend-check` for repo-local verification.
- Use `.venv/bin/python`, not bare `python`, for repo verification commands. Shell aliases are not reliable in Codex or other non-interactive shells, and no global Python alias is required.
- Before release, run `PYTHONPATH=src .venv/bin/python -m pytest`, `.venv/bin/python scripts/verify_current_baseline.py`, `.venv/bin/python scripts/verify_documentation_integrity.py`, `.venv/bin/python scripts/verify_skill_package_security_rule.py`, `.venv/bin/python scripts/verify_all.py`, `.venv/bin/python scripts/run_foundation_gate.py`, `.venv/bin/python scripts/verify_openapi_contract.py`, and `.venv/bin/python -m ruff check .`.
- Update `scripts/verify_openapi_contract.py` and the Foundation Gate when the API boundary changes.
- Keep generated OpenAPI JSON out of git unless a release explicitly asks for a versioned schema snapshot.
- Never move existing release tags or force-push release history. Create the next release tag only after verification passes and the remote tag is confirmed absent.

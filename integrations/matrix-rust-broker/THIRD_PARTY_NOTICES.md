# Third-party notices

The broker uses the Matrix Rust SDK 0.18.0, licensed under Apache-2.0, plus the
Rust crates resolved and checksum-pinned by `Cargo.lock`. The upstream source is
<https://github.com/matrix-org/matrix-rust-sdk>.

This notice is informational and does not alter the licenses of the listed
dependencies. The generated lockfile is the authoritative dependency/checksum
record for this component.

# Ultimate AI Agent Version

Current active baseline: **v0.80.0**

v0.80.0 implements M76 OpenWebUI Runtime Bridge v1. It adds deterministic
review-only OpenWebUI runtime bridge envelopes over safe refs only, policy
validation, request validation, receipt plans, stable reason codes,
documentation-integrity checks, static verification, and Foundation Gate
coverage.

It adds no live OpenWebUI connection, OpenWebUI runtime call, OpenWebUI handoff
execution, provider call, model call, model authority, tool execution, memory
write, context injection, network call, credentials or cookies, raw prompt, raw
provider payload, raw content, backend routes, Control Center controls,
dependencies, M77 work, or production authority.

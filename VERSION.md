# Ultimate AI Agent Version

Current active baseline: **v1.0.0**

v1.0.0 implements M96 Plugin Execution Sandbox, No External Plugins. It adds
a deterministic built-in test plugin sandbox contract with exact
plugin/action/permission refs, manifest permission checks, prior plugin review
binding, audit receipt, revocation binding, safe output refs, evaluator
revalidation, documentation-integrity checks, static verification, tests, and
Foundation Gate coverage.

It adds no external plugin loading, marketplace plugin, arbitrary plugin code,
runtime import, networked plugin fetch, plugin secret access, raw plugin
payload, shell execution, network access, browser automation, filesystem
mutation, model provider call, memory write, context injection, backend route,
Control Center control, dependency, M97 work, recurring automation, broad
autonomy, or production authority.

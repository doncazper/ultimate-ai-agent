# Ultimate AI Agent Version

Current active baseline: **v0.41.0**

v0.41.0 implements M37 Review Approval Capture, Review-Only Persistence. It
adds exact-packet review approval capture contracts, safe review-only approval
and denial records, narrow safe-ref-only persistence, replay/idempotency
protection, one backend capture route, Control Center review-only capture
controls, tests, static verification, documentation-integrity checks, and
Foundation Gate coverage.

It adds no raw file reads, raw file content output, raw file content storage,
full-file reads, unredacted preview, file picker/browser/upload/root selector,
export/download/copy-raw controls, context proposal, context injection, memory
writes, execution/tool controls, file mutation outside the review approval
store, dependencies, M38 work, or production authority.

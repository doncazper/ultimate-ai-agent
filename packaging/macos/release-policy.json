{
  "schema_version": "uaa.macos.release-policy.v1",
  "product_line": "ultimate-ai-agent.current",
  "stable_tag_pattern": "^v0\\.[0-9]+\\.[0-9]+$",
  "dev_tag_pattern": "^v0\\.[0-9]+\\.[0-9]+-[A-Za-z0-9][A-Za-z0-9._+-]*$",
  "retired_tag_patterns": [
    "^v1\\.",
    "^v2\\."
  ],
  "selection_clock": "tag_commit_timestamp",
  "default_channel": "newest",
  "notes": [
    "Only GitHub Releases carrying a valid active-product-line descriptor are installable.",
    "Historical v1 and v2 audit tags are never current release candidates.",
    "Changing the active product line requires an explicit policy change, not an installer rewrite."
  ]
}
